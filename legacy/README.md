# legacy/ — 已封存、不再維護

本目錄保存舊版單檔查核儀，**已封存、不再維護；前端唯一介面為 `code/frontend`**。

- `fake-news-detector.html`：舊版單檔查核儀（前端啟發式＋範例資料，可離線雙擊開啟）。
- `_run_detector.bat`：以 `python -m http.server 8090` 在本目錄提供上述頁面。

一鍵啟動腳本（`start.bat`／`start.sh`）不再提及或啟動這兩個檔案。
