# 測試計畫書

## 1. 測試計畫名稱與編號

| 項目 | 內容 |
|------|------|
| 計畫名稱 | 全民查證公社 AI 假訊息與詐騙查證系統 測試計畫書 |
| 計畫編號 | `TP-FNV-2026-01` |
| 版本 | v1.0（第 1–6 節完成；第 6 節已依 2026-09-16 實測結果填入，6.4 簽核欄待審查會議完成） |
| 日期 | 2026-09-16 |
| 受測系統 | 全民查證公社：網站前端 `https://fakenewsverify.vercel.app`、FastAPI 後端 v0.3.0（執行於負責人電腦）、Threads 機器人 `@factcheck_tw_bot`（報告週為模擬模式） |
| 測試輪次 | 報告前輪次：2026-09-15～2026-09-20（進度報告 2026-09-21～09-23 之間）；報告後輪次：延後項目，目標日期見 4.5 |
| 撰寫與審查 | 撰寫：石岱勳（負責人），由 AI 程式開發助理 Claude Code 協助；審查：廖昱翔、指導教授（見 6.4） |
| 撰寫依據 | `docs/rebuild/00_consensus.md` §7；`docs/rebuild/01_spec.md` v1.3 §1、§4、§10、§11；`docs/rebuild/03_tickets.md` §0.5；`docs/rebuild/owner_decisions_day0.md` |

**修訂紀錄**

| 版本 | 日期 | 內容 |
|------|------|------|
| v0.1 | 2026-09-15 | 第 1–3 節初稿 |
| v0.9 | 2026-09-16 | 章節名稱依課程範本；第 1–3 節同步現況（CGU AIR `gpt-5.4-mini`、Vercel 網址、Threads 模擬模式、D-05 清洗結果、parquet 原子寫入修正）；第 3 節各表新增「本次執行」欄；OP-1、OP-9、FN-1 準則依負責人核准事項修訂、QA-1b 評測日期更正（見 4.2）；完成第 4、5 節；建立第 6 節結構 |
| v1.0 | 2026-09-16 | 第 6 節填入 2026-09-16 實測結果（6.1 摘要與整體判定、6.2 全部 46 項明細、6.3 缺陷 DEF-01～DEF-08、6.4 審查結論與簽核欄）；§5.1 組員姓名與分工定案（票 O-10）；匯出 `docs/test/TP-FNV-2026-01_v1.0.pdf`。本輪未達 4.2 之「全部 P0 通過」與「P1 ≥80%」，理由見 6.1 |

**本計畫各節與規格書之對應**

| 本計畫章節 | 來源 |
|------------|------|
| 2.1 計畫目的、2.2 計畫範圍 | spec §1（成功定義、非目標）、§4；`03_tickets.md` §0.5 |
| 2.3 參考文件 | spec §10.6「2.3 參考文件」，補列報告週決策與執行紀錄 |
| 3.1–3.5 測試項目 | spec §10.1–§10.5（FR 編號引用 spec §4） |
| 4.1 測試環境需求、4.4 測試交付項目 | spec §10.6，依 2026-09-16 實際環境調整 |
| 4.2 測試項目的通過準則、4.3 測試中止的準則與再繼續的需求 | spec §10 開頭與「4.2 通過準則彙總」 |
| 4.5 測試工作項目與時程 | spec §11；`03_tickets.md` §0.5、§10.2 |
| 5 人員配置與訓練需求 | 共識 §1（團隊 5 人）、spec §11 |

## 2. 簡介

### 2.1 計畫目的

驗證「全民查證公社」重做版本（2026-09-15 起一週重做）是否達到進度報告與 3 分鐘 demo 影片的交付條件，並以可量測、可重跑的準則留下證據，供進度報告與論文引用。

**產品定位**（spec §1）：台灣繁中的假訊息／詐騙查證平台——貼上文字或網址（圖片為 P1），AI 判定 SCAM／MISINFO／SAFE 並附查核來源；同一套判定在 Threads 上 @機器人 即可取得，網站結果可一鍵分享回 Threads。

**測試目標**（spec §1 成功定義，依報告週範圍調整）：

1. 進度報告與 3 分鐘 demo 影片如期交出。
2. 影片完整呈現閉環：Threads 可疑貼文 → Tester 回覆並 @機器人 → 機器人回覆紅／黃／綠燈號＋來源＋`/r/{id}` 連結 → 手機版結果頁 → 「分享到 Threads」。報告週 Threads 採模擬模式（FR-10）呈現同一閉環，實機串接排在報告後。
3. 網站四個主畫面（首頁／結果頁／熱門牆／知識庫）在手機寬度可用；淺色為預設；紅黃綠只出現在判定。
4. AI 實際可用（CGU AIR `gpt-5.4-mini`，provider 鏈 `['cgu']`）：至少一次「未命中快取 → AI 判定」與一次「改寫版命中向量快取（`cache_layer="vector"`）」；全程無「AI 分析暫時無法使用」畫面。
5. `pytest tests` 全綠、CI 通過；評測數字（前一版模型 accuracy 96.0%／FN=0 與 CGU `gpt-5.4-mini` 新跑值並列）與三層快取證據可供報告引用。
6. demo 資料乾淨：畫面上每一個「查核來源」皆為 Tier 1／2；熱門牆無 Google News 轉址；知識庫頁不出現 `verified=false` 記錄；找不到查核來源時明示「尚無查核機構證實」且不出現綠燈。

### 2.2 計畫範圍

**測試範圍**：spec §4 FR-01～FR-19 中列為 P0 者為必測；P1 條目依 4.2 之通過率計入；P2 條目（FR-12 每日查核貼文、`/history` 全頁、速率限制）不在範圍內。測試分為操作（3.1）、功能（3.2）、介面（3.3）、績效（3.4）、品質（3.5）五類，共 46 項（P0 30 項、P1 16 項）。

**本輪與報告後之劃分**（`03_tickets.md` §0.5，2026-09-15 負責人選定「報告週切片 A」；各項目歸屬見第 3 節「本次執行」欄）：

| 輪次 | 範圍 |
|------|------|
| 本輪（報告前） | 網站（首頁、結果頁、熱門牆、知識庫、靜態頁）；三層快取與 AI 判定；來源分級與資料清洗（D-05 已套用）；Threads 模擬模式閉環（終端機輸出＋`replies.jsonl`）；自動化功能測試與 CI；評測、績效與成本量測；以 headless 瀏覽器擷取之介面截圖檢核；公開部署與發佈前操作測試 |
| 報告後 | Threads 實機串接（Meta App、Threads Tester、長效 token、live 回覆與延遲）；機器人狀態頁 `/bot`（票 S-11）；真手機測試（含 Threads App 內建瀏覽器）；無障礙量測；P1 功能（圖片查證、`similar_news`、AI 額度護欄程式）；文件同步 |

**非目標（不測）**（spec §1、共識 §2）：影片查證（yt-dlp＋Whisper）、FB／IG 封閉平台（Playwright）、文字輸入的 Google 搜尋抓取、熱門牆的 Google News 來源、管理者覆寫 UI（`/api/admin` API 保留）、使用者帳號／登入、英文介面、Threads Webhook、Threads 私訊、公開讓陌生人 @機器人（需 App Review＋企業驗證，論文寫明為上線條件）。

**受測系統範圍**（2026-09-16 狀態）

| 元件 | 說明 | 對應 FR |
|------|------|---------|
| 網站前端 | React 19＋Vite 8.3.0＋Tailwind 4＋react-router-dom 7（`code/frontend`），部署於 Vercel（`https://fakenewsverify.vercel.app`，即 `PUBLIC_BASE_URL`）。路由：首頁 `/`、結果頁 `/r/{id}`、熱門牆 `/trending`、知識庫 `/knowledge`、授權碼顯示 `/oauth/callback`；純 HTML 靜態頁 `/privacy.html`、`/data-deletion.html`、`/deauthorize.html`。`/bot` 目前為佔位頁，正式機器人狀態頁（S-11）排在報告後。前端只讀後端 `frame_type`／`frame_label` 顯示燈號；查證歷史存 localStorage（首頁最近 5 筆）。依第二批刪減，熱門牆篩選只留「全部／未查證」，知識庫單次讀取 60 筆（無「載入更多」與四格統計）。 | FR-04～08、FR-11、FR-13 |
| 後端 API | FastAPI＋Uvicorn（`code/backend`，v0.3.0，單 worker），執行於負責人電腦，經 Cloudflare quick tunnel 對外；Vercel `vercel.json` rewrites 將 `/api/*` 同源代理至 tunnel。查證送出後立即取得 `result_id`，結果頁輪詢 `GET /api/result/{id}`。管理端點以 `ADMIN_TOKEN` 保護（`start.bat` 自動產生）。 | FR-01、FR-02、FR-04、FR-15 |
| 三層快取 | 文字輸入：L1 內容 SHA-256 → L2 向量（`text-embedding-3-small` 1536 維，以使用者原文比對，門檻 0.75，只比對 `verified=true` 列）→ L3 AI；不送 Google 搜尋。網址輸入：L0 URL → L1 hash → trafilatura 爬取（SSRF 防護 `safe_url.py`）→ L2 向量（以爬到內文比對）→ L3 AI。結果回填 `knowledge_base.parquet`；回應帶 `cached`／`cache_layer`（url／hash／vector）。 | FR-01、FR-02、FR-17 |
| AI 引擎 | CGU AIR Gateway（OpenAI-compatible Responses API；`AI_PROVIDER=cgu`、`CGU_MODEL=gpt-5.4-mini`、`OPENAI_REASONING_EFFORT=low`、單次逾時 60 s），provider 鏈 `['cgu']`；新學期額度為 OpenAI 成本 USD 10＋本地模型 1,000 萬 tokens，用量以 `GET {CGU_BASE_URL}/me/usage` 查詢；不使用 OpenAI 直連，myai168 已停用。provider 全失敗時回 HTTP 200 fallback（`ai_unavailable=true`、summary 以「AI 分析暫時無法使用」開頭），不寫知識庫。系統 prompt 含來源規則（FR-19）；`allowed_domains` 為 P1，本輪不實作。 | FR-01、FR-14、FR-19 |
| 來源分級 | `app/utils/source_tier.py`：Tier 1 為已有判定之查核機構（TFC、MyGoPen、有 RUMOR／NOT_RUMOR 回覆之 Cofacts 文章、`*.gov.tw` 等官方機構）；Tier 2 為主流媒體查核報導（呼叫 `_title_indicates_debunk()`）；Tier 3 為其餘。對外只顯示 Tier 1／2；無 Tier 1／2 來源時 `verified=false`，顯示「尚無查核機構證實」且不得為綠燈。 | FR-16、FR-17 |
| 資料清洗 | `scripts/clean_sources_2026_09.py`（預設 dry-run、冪等、apply 前自動備份）。D-05 於 2026-09-16 經負責人核准後套用（決定 1a／2a／3a）：知識庫 236 → 218 筆，`verified=true` 128 筆（其中 113 筆帶向量；實測僅 102 筆為 1536 維、可實際參與向量命中，見 6.3 DEF-05）；熱門牆 63 → 24 筆（刪除 Google News 轉址 31 筆與髒資料 8 筆），全數 `verified=true`。審閱紀錄見 `docs/test/clean_sources_review.md`。 | FR-18 |
| 紅黃綠映射 | `app/utils/verdict.py`：`frame_of()` 為單一權威（spec §7.8，8 列），`is_fallback()` 為 fallback 唯一判斷點，Web 結果頁與 Threads 回覆共用。 | FR-04、FR-09 |
| Threads 機器人 | `@factcheck_tw_bot`；`THREADS_MODE=live／sim／off`。**報告週為模擬模式**：`FakeThreadsService` 讀 `data/threads_sim/mentions.json`、寫 `data/threads_sim/replies.jsonl`，分析管線、回覆模板（≤500 字，spec §7.7）、去重與 state 原子寫入皆與實機相同；以 `scripts/test_threads_bot.py --reset-sim`／`--poll` 或帶管理金鑰之 `POST /api/threads/poll`（202）觸發，`GET /api/threads/status`、`GET /api/threads/replies` 提供狀態與回覆。實機模式需 Meta App（Threads use case 四項權限）、公開之 Threads Tester 帳號與 60 天長效 token，開發模式下只讀得到 Tester 的提及與貼文，排在報告後。 | FR-09、FR-10、FR-11 |
| 分享 | 結果頁「分享到 Threads」以 Web Intent `https://www.threads.com/intent/post?text=…&url=…` 開新視窗，純前端、零 token。 | FR-05 |
| 資料 | SQLite `factcheck.db`（熱門牆 24 筆，不進 git）；`knowledge_base.parquet`（218 筆，清洗後種子已提交，commit `db8c62d`）；`tasks.parquet`（結果頁，上限 5,000 筆；自 commit `c114413` 起先寫暫存檔再原子替換）；`eval_set.csv`（150 筆評測集）。 | FR-06、FR-07、FR-18 |

### 2.3 參考文件

1. `docs/rebuild/00_consensus.md`（重做共識，2026-09-14，含 2026-09-15 §4／§9 補充）
2. `docs/rebuild/01_spec.md`（功能與 UX 規格書 v1.3）
3. `docs/rebuild/02_mockup_brief.md` 與 `docs/rebuild/mockup/*.dc.html`（設計簡報與 mockup；UI-1 對照）
4. `docs/rebuild/03_tickets.md`（實作票 v1.1；§0.5 報告週範圍）
5. `docs/rebuild/owner_decisions_day0.md`（負責人決策紀錄）
6. `docs/rebuild/runbook_tunnel.md`（tunnel 啟動與 `vercel.json` 換主機程序）
7. `CLAUDE.md`（專案說明；§6 評測現況）
8. `code/backend/data/eval_archive_gpt5mini_2026-06/`（前一版模型 `gpt-5-mini` 150 筆評測之 `eval_report.csv`、`eval_binary.csv`、`eval_errors.csv`、`eval_predictions.csv`、`confusion_matrix.png`，2026-06-06，commit `9d63eef`；本輪新結果寫回 `code/backend/data/eval_*.csv` 與 `assets/confusion_matrix.png`）
9. `code/backend/data/clean_sources_report.txt`、`check_before_d05.txt`、`check_after_d05.txt`、`clean_sources_drop_ids.txt`（FR-18 清洗報告、前後比對與核准刪除清單）
10. `docs/test/clean_sources_review.md`（D-05 清洗審閱紀錄；QA-5）
11. `docs/demo/storyboard.md`、`docs/demo/footage_log.md`（demo 影片分鏡與素材紀錄）
12. Threads 官方文件（Threads API Overview、Reference > Publishing／Replies／Mentions、Get Started > Permissions／App Roles、Web Intents；以 threads_gap_analysis／threads_verify_10 記錄之 URL 為準）
13. CGU AIR Gateway 教學頁（`https://air.cgu.edu.tw/workspace4/LLMAPI/api_call.html`；OpenAI-compatible Responses API，`/v1/me/usage` 用量查詢）
14. OpenAI Responses API 文件（僅作格式參考；`web_search` `filters.allowed_domains` 於閘道之支援性見 OP-2）

## 3. 測試項目

各表欄位：ID／優先級／本次執行／對應 FR／（操作測試另有「步驟」）／通過準則。ID、優先級與準則文字與 spec §10 一致；v0.9 修訂處以〔v0.9 修訂〕標示，理由見 4.2。「對應 FR」欄在 spec 功能測試表已列者照抄，其餘依準則內容對應 spec §4 之 FR 或章節。

「本次執行」欄：**本次執行**＝於報告前輪次執行，結果記於第 6 節；**報告後執行（原因）**＝依報告週切片 A（`03_tickets.md` §0.5）或功能優先級延後，目標日期見 4.5。本次執行項目若有部分準則依附報告後才實作之功能，於各表下方「註」列明，判定方式見 4.2。

### 3.1 操作測試

操作者：石岱勳（負責人）；腳本執行可由 Claude Code 在其監督下進行。來源：spec §10.1。

| ID | 優先級 | 本次執行 | 對應 FR | 步驟 | 通過準則 |
|----|--------|----------|---------|------|----------|
| OP-1 | P0 | 本次執行 | FR-15、FR-18、§5.6 | 全新 clone → `start.bat` | 後端 `/health` 200 且 `ai_available=true`；前端 5173 開啟首頁；<5 分鐘（含 pip/npm）；`.env` 已自動產生 ≥32 字 `ADMIN_TOKEN`，`curl -X POST -H "X-Admin-Token: …" /api/threads/poll` 回 202 或 200 `threads_disabled`；**`GET /api/knowledge/stats.total ≥ 125`**（證明提交的種子是 FR-18 清洗後版本，`verified` 已回填；在 OP-9 之後執行）〔v0.9 修訂：原 ≥150〕 |
| OP-2 | P0 | 本次執行 | FR-19、FR-14 | `scripts/test_ai_provider.py --provider cgu`（關／`--web-search` 開／`--web-search --allowed-domains` 各跑一次；前後各查一次 `GET {CGU_BASE_URL}/me/usage`） | **P0 條件（只有這一項擋交付）**：關閉 web_search 的一次印出合法 `risk_type`、非 fallback、印出 `usage`，embedding 維度 1536。**記錄項（回 400／不支援不算不達標）**：`--web-search` 與 `--allowed-domains` 兩次結果寫入 `docs/test/cgu_usage.txt`——`--web-search` 回 400／閘道不支援 → `USE_WEB_SEARCH=false` 進 demo 並在報告註明；`--allowed-domains` 回 200 且 `url_citation` 全在清單內（另記 `gov.tw` 裸網域是否涵蓋子網域）→ 閘道支援，FR-19 維持 P1，由 D-16 決定是否於報告後實作（票 B-24）；否則不做。三次 `/me/usage` 前後差合計 ≤ USD 0.06 |
| OP-3 | P0 | 本次執行 | FR-10、FR-11 | `THREADS_MODE=sim` + `scripts/test_threads_bot.py --poll`（或 `curl` 打 `/api/threads/poll` 後輪詢 `status`） | 一輪完成、`replies.jsonl` 新增 ≥1 行、`/bot` 頁 ≤4 秒顯示該回覆、`status.mode == "sim"` |
| OP-4 | P1 | 報告後執行（需 Meta App、Threads Tester 與實機授權） | FR-09（§7.3） | `scripts/threads_auth.py` 走完授權 | `data/threads_token.json` 產生，含 `authorized_at`，`token_days_left` 介於 55–60 |
| OP-5 | P1 | 報告後執行（需 Threads 實機 token 與 Tester 帳號） | FR-09（§7.6、§7.7、§7.10） | `scripts/test_threads_bot.py --live` | profile 與 mentions 皆 200；log 一則真實 mention 原始 JSON（記下 `media_type` 實際值）；對機器人自己貼文試發 `auto_publish_text=true` 回覆一則並 `GET /{id}` 可讀（7.6 D-11 依據）；480 字純中文 + 3 emoji 試發一則（7.7 長度規則）；故意讀非 Tester 貼文、用壞 token 各一次並記錄狀態碼與 `error` body（7.10） |
| OP-6 | P1 | 報告後執行（需實機 token 取得滿 24 小時） | FR-09（§7.3） | `scripts/threads_auth.py --refresh` | `expires_at` 延後 ≥50 天（或拒絕：token <24 h） |
| OP-7 | P0 | 本次執行 | FR-15、§6 | 以 v1.0 前的舊 `tasks.parquet`／`knowledge_base.parquet`／`factcheck.db`（快照 `docs/test/op7_legacy/`）啟動後端 | 載入成功、新欄位為預設值、筆數不變（`check_db.py` 前後比對） |
| OP-8 | P0 | 本次執行 | FR-04、§8.2 | Vercel 部署 + quick tunnel + `vercel.json` rewrites | 手機 4G 開 `{PUBLIC_BASE_URL}/r/{任一 id}` 可看到結果；`{PUBLIC_BASE_URL}/api/health` 200（同源代理） |
| OP-9 | P0 | 本次執行 | FR-18 | `scripts/clean_sources_2026_09.py --dry-run` → 負責人審閱 `data/clean_sources_report.txt`（D-17）→ `--apply --drop-ids data/clean_sources_drop_ids.txt` → 再 `--apply` 一次 | dry-run 不改任何檔（`git status`／檔案 mtime 不變）；報告列出各規則影響筆數與逐筆 before→after；apply 後 `check_db.py`：知識庫筆數 = 236 − 核准刪除清單中存在於種子之筆數、`verified=true` ≥125、熱門牆 `news.google.com` 0 筆、`.bak-{date}` 備份存在；第二次 apply 影響筆數 0（冪等）；`pytest tests` 仍綠〔v0.9 修訂：原「知識庫 236 筆不變、`verified=true` ≥150」〕 |

註：
1. OP-3 之「`/bot` 頁 ≤4 秒顯示該回覆」依附機器人狀態頁（票 S-11，報告後），本輪以其餘準則判定；報告週 demo 影片以終端機輸出與 `replies.jsonl` 呈現機器人回覆（`docs/demo/footage_log.md`）。
2. OP-2 前置：`test_ai_provider.py` 須具備 `--web-search`／`--allowed-domains` 旗標與 `usage` 輸出（票 B-02）。
3. OP-1、OP-7 於獨立資料夾之全新 clone 執行，不複製原 `.env`（金鑰由石岱勳自行填入），OP-7 將舊檔快照複製至新 clone 之 `code/backend/data/`（票 O-25 做法）。
4. OP-8 前置：啟動 cloudflared quick tunnel，依 `docs/rebuild/runbook_tunnel.md` 將主機寫入 `vercel.json` 並部署 Ready（票 O-08）。

### 3.2 功能測試

執行方式：`cd code/backend && .\venv\Scripts\python -m pytest tests -q`（本機）與 GitHub Actions（CI 自動）。來源：spec §10.2。

| ID | 優先級 | 本次執行 | 對應 FR | 通過準則 |
|----|--------|----------|---------|----------|
| FN-1 | P0 | 本次執行 | FR-01、FR-19 | 既有測試依 5.5／7.7／7.8 新契約更新後全過（**允許改寫** `tests/test_ai_service_contract.py` 的框色案例（改驗 7.8 表 **8 列**）與 Threads 回覆案例（改驗 7.7 新模板，含「尚無查核機構證實」替代行）；其餘 `test_cache_and_store`／`test_marking_rules`／`test_api`／`test_url_validator`／`test_processor_flow` 五檔**不得修改斷言**〔v0.9 修訂：經負責人同意閘門 O-01，`test_processor_flow.py`（B-03，移除文字輸入關鍵字爬取）與 `test_api.py::test_health_and_root`（B-22，改為只比對 `status`）兩處例外允許修改〕——`test_cache_and_store` 的 fixture 直接呼叫 `save_record`、無 `verified` 參數，其來源 `165.npa.gov.tw` 為 `gov.tw` Tier 1，由 `save_record` 內部離線計算得 `verified=true`（FR-17「計算位置」），`test_vector_search_exact_hit` 在 `find_similar_by_vector` 加 `verified` 過濾後仍原樣通過）+ 新增：文字輸入不呼叫 crawler（mock 斷言 `process_input` 未被呼叫）；系統 prompt 含 FR-19 逐字規則（字串比對「找不到這類來源就回 `sources: []`」） |
| FN-2a | P0 | 本次執行 | FR-02 | `test_safe_url.py`：10 個私網／協定樣本全部拒絕（422 `blocked_url`）且未發出請求、3 個公網放行；連線逾時 10 s 與總逾時 `CRAWLER_TIMEOUT` 以 mock 各自驗證生效；爬取失敗回 UNVERIFIABLE 而非例外；非網址 422；輸入網址同網域的 `sources` 項被剔除。（`similar_news` 向量近鄰斷言隨功能降 P1 移至 FN-2c） |
| FN-2c | P1 | 報告後執行（`similar_news` 向量近鄰為 P1，本輪固定回 `[]`，D-18） | FR-02 `similar_news` | `similar_news` 由向量近鄰填入（固定向量 fixture）、只取 `verified=true` 列、排除自己、≥0.6 |
| FN-2b | P1 | 報告後執行（圖片查證 FR-03 為 P1，端點 B-26 報告後實作） | FR-03 | 非圖片 415、>10 MB 413、bytes hash 命中 |
| FN-3 | P0 | 本次執行 | FR-04、5.1 | `test_result_api.py`：`/api/result/{id}` 對 pending／completed／failed／404／ai_unavailable 五態 schema 正確；sync／async／threads_sim 三種來源皆 200；`/health` 含 `scheduler.{enabled,interval_hours}` |
| FN-4 | P0 | 本次執行 | FR-09／FR-10 | `test_threads_bot_sim.py`：文字 mention 產生 `threads_len ≤ 400` 回覆且含 `/r/`；圖片 mention（`media_type=IMAGE`、無文字）→ `reply_media_only`；`get_post` HTTP 403 → `reply_cannot_read`；HTTP 429 → `backoff_until` 設定；未知 4xx → 標 failed 不回覆；二次 poll 不重複；fallback 不回覆不標記；自我貼文跳過（`username == bot` 與 `reply_id` 在本機清單兩種）；每則後 state 已寫入（crash 模擬）；container `FINISHED` 才 publish、`ERROR` 重試一次 |
| FN-5 | P0 | 本次執行 | 7.7 | **七種**第一行模板（含黃燈符號＋「尚無查核機構證實」）`threads_len ≤ 400`（實測後放寬 480）；4 個 emoji + 200 字 summary、含警示符號（U+26A0 U+FE0F）之 summary 兩個極端案例仍 ≤500 且保留結果頁連結；「黃燈＋尚無查核機構證實」第一行＋替代行「尚無查核機構證實」（無連結）一組長度案例：連結數 = 1（只剩結果頁）、替代行計入長度不計連結；連結數 ≤2；`threads_len` 對 BMP emoji／U+FE0F／U+200D 以 bytes 計；**實測長度規則紀錄欄**（OP-5 實測結果） |
| FN-6 | P0 | 本次執行 | 7.8 | `frame_of()` 對表格 **8 列** + 11 組 (is_risk, risk_type, confidence, ai_unavailable, verification_status) 輸入的表格測試；含「SAFE、0.95、unverified → yellow 尚無查核機構證實」與「SAFE、0.95、verified → green」、「缺 `verification_status` 鍵 → 視為 unverified」三組 |
| FN-7 | P0 | 本次執行 | 5.5 | `is_fallback()` 統一判斷（含 `evaluate.py`／`test_ai_provider.py` 改呼叫）；`ai_unavailable` 與 summary 前綴同時成立 |
| FN-8 | P0 | 本次執行 | FR-07 | `q="("` 200；`offset` 分頁不重複不遺漏；stats 四格加總 = total；fixture 中 `verified=false` 列不出現在列表、不計入 `total`（FR-17） |
| FN-9 | P0 | 本次執行 | 5.6 | 管理端點無 token → 401；`ADMIN_TOKEN` 空 → 403；正確 → 202/200/409 |
| FN-10 | P1 | 報告後執行（AI 額度護欄 FR-14 程式為 P1，報告後實作） | FR-14 | `DAILY_AI_CALL_CAP=1` 時第二次未命中回 429 且 AI mock 未被呼叫 |
| FN-11 | P0 | 本次執行 | 三層快取 | 同文 hash 命中、改寫文 vector 命中（固定向量 fixture）；`verified=false` 列即使相似度 0.99 也不命中 vector 層（hash 層仍命中）；圖片 bytes hash 命中移至 FN-2b |
| FN-12 | P0 | 本次執行 | FR-16 | `test_source_tier.py`：≥15 組 URL／標題 → tier 表格測試（`gov.tw.evil.com` → 3、`news.google.com/rss/…` → 3、Cofacts 有回覆 mock → 1／無回覆 → 3、MyGoPen 任意標題 → 1、ETtoday 查核標題 → 2、ETtoday 一般標題 → 3、Cofacts GraphQL 逾時 → 3、`offline=True` 時 Cofacts → 3 且未發請求）；GraphQL mock 對準 `https://api.cofacts.tw/graphql`（與 search_service.py:216 同端點）；`tier_of` 對 Tier 2 呼叫的是 `news_fetcher._title_indicates_debunk`（mock 斷言，防複製規則） |
| FN-13 | P0 | 本次執行 | FR-17 | `test_kb_write_gate.py`：FR-17 驗收 1 的四組（Tier 1 存活 → true；只有 Tier 3 → false 且 vector 不命中；`sources=[]` + rule → true；Tier 1 但 validator 判死 → false）；`/api/result` 回 `verification_status` 三值正確；`sources[]` 無 tier 3 |
| FN-14 | P0 | 本次執行 | FR-18 | `test_clean_sources.py`：fixture Parquet／SQLite 上 `--dry-run` 檔案 mtime 不變；`--apply` 兩次結果相同；Google News 解析 mock 成功（覆寫 + 去重）與失敗（刪列）兩種；`label_source="rule"` 列永不降級 |

整體目標（spec §10.2「目標」列，非獨立測試 ID）：P0 測試數 ≥60、CI 綠燈、執行 <60 s、零付費呼叫（Cofacts GraphQL 一律 mock）；FR-01／02／04～07／09／10／11／16／17／18 每條至少一個測試。

註：
1. FN-4 之「HTTP 429 → 設定 `backoff_until`」「未知 4xx → 標 failed 不回覆」「container `FINISHED` 才 publish、`ERROR` 重試一次」三組案例屬實機發佈與錯誤分流（票 T-12、T-13，報告後），本輪以其餘案例判定。
2. FN-5 之「實測長度規則紀錄欄」為記錄項，依附 OP-5 實機實測（報告後）；本輪長度上限採 spec §7.7 預設值。
3. 前端純 JS 模組之單元測試（`npm run test:unit`）隨 CI 執行，作為介面邏輯之輔助證據，不另立測試 ID。

### 3.3 介面測試

執行方式：visual review（共識 §7）。本輪以 Microsoft Edge headless 擷取截圖（桌機 1280 px、手機模擬 375 px，UI-2 另以 360×780；淺色與深色），由非實作組員（姚睿）依截圖檢核表 `docs/test/ui_checklist.csv` 逐格勾核、廖昱翔覆核；真手機與 Threads App 內建瀏覽器之測試排在報告後。來源：spec §10.3。

| ID | 優先級 | 本次執行 | 對應 FR | 通過準則 |
|----|--------|----------|---------|----------|
| UI-1 | P0 | 本次執行 | §8.3、§8.4（FR-04、FR-06、FR-07、FR-11、FR-13） | 以 8.4 狀態總表為母版產出「截圖檢核表」（每格一列：畫面／狀態／寬度／主題／檔名／勾核者／結果）。P0 畫面 S1、S2、S3、S4、S6 的打勾格 × 2 寬（375／1280 px）× 2 主題 = 必交（約 100 張；S5 為 P2 不列）。「一致」定義：該格顯示 8.7 對應 key 的文案且無 8.6 列出的違規（字級 <13 px、hover-only 說明）；S1／S2 另與 mockup（`docs/rebuild/mockup/Main.dc.html`、`Result.dc.html`、`ResultStates.dc.html`）對照，S3／S4／S6 只對 8.3 元件清單。由非實作組員勾核 |
| UI-2 | P0 | 本次執行 | FR-04、FR-05、§8.5 | 手機 360×780 無橫向捲動；結果頁動作列 sticky 可見；底部分頁列在安全區內 |
| UI-3 | P1 | 本次執行 | §7.8、§8.1 | 顏色紀律：`grep -l 'c-verdict' src/**/*.jsx` 命中的元件檔 ≤3（ResultCard、Chip、燈號點）；導覽／按鈕不出現紅黃綠（截圖抽查） |
| UI-4 | P0 | 本次執行 | §8.1 | 淺色為預設；系統深色時自動深色；切換後重整保持 |
| UI-5 | P1 | 報告後執行（無障礙基準票 P-18 排在報告後） | §8.6 | Lighthouse（手機）Accessibility ≥90、Performance ≥80；axe 零 critical；鍵盤 Tab 可走完首頁並完成一次查證 |
| UI-6 | P0 | 本次執行 | §8.7 | 文案表 8.7 全部 key 在介面出現且逐字一致；`grep -P '[a-zA-Z]{4,}' src/i18n.js` 只允許品牌與 URL；簡體以 OpenCC `t2s` 轉換前後 diff 為空 |
| UI-7 | P1 | 報告後執行（需真手機與 Threads App 實機串接） | FR-04、FR-05 | Threads 手機 App 內建瀏覽器開 `/r/{id}`：燈號首屏可見；分享鈕開出的 URL 符合 FR-05 驗收 1（App 內是否深連結 composer 只記錄） |
| UI-8 | P0 | 本次執行 | FR-16 | 來源紀律（FR-16）：S2 截圖抽查 10 筆結果頁，「查核來源」區每項皆帶 `tier_1_chip`／`tier_2_chip`，無 Tier 3 網域（用 OP-9 後的資料）；S3 截圖無 `news.google.com`；至少一張 S2「尚無查核機構證實」狀態截圖（SAFE 黃 + 橫幅）入檢核表 |

註：
1. UI-1 之 S6 `/bot`（票 S-11，報告後）截圖格延後；S3 篩選 chip 僅「全部／未查證」、S4 無「載入更多」與四格統計（`03_tickets.md` §0.5 第二批刪減），相應狀態格標「不適用（範圍刪減）」。
2. UI-6 之 `/bot` 畫面文案 key 隨 S-11 延後；P1／P2 功能與隱藏區塊之 key 不列入本輪範圍（範圍界定依票 O-28，須經 6.4 審查確認）。
3. 實作票（`docs/rebuild/03_tickets.md` §0 第 4 條）沿用 mockup brief §2 的 token 名（`--c-red` 等），UI-3 的 grep 關鍵字實際執行時以該 token 名為準，判定準則（命中元件檔 ≤3、導覽／按鈕不出現紅黃綠）不變。

### 3.4 績效測試

執行方式：`evaluate.py --timing`＋後端結構化 log（`result_id`、`origin`、`cache_layer`、`elapsed_ms`）＋手動計時（依共識 §7 不新增腳本）。來源：spec §10.4。

| ID | 優先級 | 本次執行 | 對應 FR | 通過準則 |
|----|--------|----------|---------|----------|
| PF-1 | P0 | 本次執行 | FR-01、三層快取、§9 效能 | `evaluate.py --timing`（web_search 關）：未命中 p50 ≤15 s／p90 <20 s（150 筆 `elapsed_ms`）；L1／L2 命中：對 10 筆已入庫文字各重送一次、從後端結構化 log 取 `elapsed_ms + cache_layer`，L1 p50 <1.5 s、L2 p50 <4 s |
| PF-2 | P0 | 本次執行 | FR-01、三層快取 | 三層快取命中率：對 20 筆「改寫版謠言」（張宇宏準備，獨立於 eval_set）以 `POST /api/analyze/sync` 送出，log 中 `cache_layer=vector` ≥70% |
| PF-3a | P0 | 本次執行 | FR-09、FR-10 | Threads 本專案可控延遲：從 `run_threads_poll` 取得 mention 到 `reply_to` 回 200 ≤60 s（log `elapsed_ms`；sim 與 live 各量 3 次） |
| PF-3b | P1 | 報告後執行（需 Threads 實機 @ 回覆） | FR-09 | Threads 端到端牆鐘時間（含 Meta 索引延遲，不可控）：使用者送出 @ 到回覆出現，記錄 5 次實測，目標 ≤ poll 間隔 + 60 s（demo 設 1 分鐘 → ≤2 分鐘）；未達標只記錄不擋 |
| PF-4 | P1 | 報告後執行（本週無對應票；模擬 fixture 僅 6 則，需補足 10 則） | FR-10 | 模擬模式 10 則 mention 一輪 <3 分鐘（AI 開）；mock AI 時 <10 s |
| PF-5 | P1 | 本次執行 | FR-14、§9 成本 | 成本：150 筆評測總花費 ≤ USD 1.5；每次未命中平均 ≤ USD 0.02（CGU `GET /me/usage` 評測前後差 + OP-2 `usage` 輸出，記入 `docs/test/cgu_usage.txt`） |
| PF-6 | P1（可選） | 報告後執行（可選比較組；spec FR-14 將其預算保留至報告後） | FR-01（模型比較） | 模型比較組：同 150 筆以 `CGU_MODEL=gpt-5.4` 重跑一次（`USE_WEB_SEARCH=false`；預算 ≤ USD 2，先以 `/me/usage` 確認餘額 ≥ USD 4 才跑），與 `gpt-5.4-mini` 並列 accuracy／FN／p50 延遲／每筆成本；報告 §6 一張表；不作通過條件 |

註：
1. PF-3a 本輪量測模擬模式 3 次；live 3 次隨 Threads 實機串接於報告後補量。
2. 前置：PF-1 須 `evaluate.py --timing`（票 B-23）；PF-2 須改寫謠言 20 筆 `docs/test/pf2_paraphrases.csv`（票 O-11）。
3. 會寫入資料之量測（PF-2、PF-3a、QA-1a 之 `--seed-db`）依序執行，不與其他寫入程序並行（單寫者假設，spec §12 R10），執行前依 4.3 備份資料檔。

### 3.5 品質測試

執行方式：`evaluate.py`（150 筆 `eval_set.csv`，SCAM／MISINFO／SAFE 各 50）；QA-3 依 GitHub Actions；QA-4、QA-5 依審查紀錄。來源：spec §10.5。

| ID | 優先級 | 本次執行 | 對應 FR | 通過準則 |
|----|--------|----------|---------|----------|
| QA-1a | P0（閘門） | 本次執行 | FR-01 | `evaluate.py --seed-db`（web_search 關）以 **CGU `gpt-5.4-mini`**（provider 鏈 `['cgu']`）重跑 150 筆：**FN=0（絕對條件）**；跑前後各查 `/me/usage`（PF-5） |
| QA-1b | P1（量測） | 本次執行 | FR-01 | 同一次執行的 accuracy 與 FP 記錄實際值，目標 ≥93%／≤8；未達標不擋交付，但報告 §6 標示「未達標」並附錯誤分析。報告依 D-8 並列舊數字（0.96，gpt-5-mini via myai168，2026-06〔v0.9 修訂：原記 2026-07，依 commit `9d63eef` 更正〕）與新數字（gpt-5.4-mini via CGU AIR，2026-09），各標 provider／model／date；PF-6 比較組（gpt-5.4）有跑才加第三欄 |
| QA-2 | P1 | 本次執行 | FR-01（評測報告） | `evaluate.py` 報告加 `provider/model/date/use_web_search/elapsed_ms` 欄；`--report-only` 重算一致；`eval_errors.csv` 逐筆有 `error_type` |
| QA-3 | P0 | 本次執行 | FR-15 | CI 每次 push 綠燈（`pytest` + `npm run build`）；`ci.yml` 不再寫死測試數 |
| QA-4 | P1 | 報告後執行（文件同步票 B-25 排在報告後） | FR-15、FR-16 | CLAUDE.md、README、`.env.example` 與本 spec 的端點／設定名一致（文件審查勾核表，含：Threads 權限清單修正、§2 表格「影片語音轉文字」列刪除、移除 `ENABLE_THREADS_BOT`／STT 設定鍵、`legacy/` 標示、**§2 AI 引擎改寫為「CGU AIR 為唯一 provider、myai168 已停用、額度查 `/me/usage`」**、§9 加來源分級三條規則、`BOT_HANDLE=factcheck_tw_bot`、**§4／§7「runtime 變動不用 commit」補例外：FR-18 清洗後的 `knowledge_base.parquet` 提交一次作新種子**） |
| QA-5 | P0 | 本次執行 | FR-18 | 清洗 dry-run diff 審查（FR-18）：`data/clean_sources_report.txt` 中 0 筆 `label_source="rule"` 被降級；隨機抽 10 筆降級列由負責人逐筆判斷，同意 ≥9（不同意者記回 `TIER1_DOMAINS`／規則修正後重跑 dry-run）；Cofacts 33 筆逐筆有「有回覆／無回覆」結論；Google News 31 筆逐筆有「解析成功→新網址／失敗→刪除」結論；審查紀錄存 `docs/test/clean_sources_review.md` |

註：
1. 前置：QA-2 之報告欄位與 PF-1 之 `--timing` 同屬票 B-23。
2. QA-1a 之 `--seed-db` 會把判對案例以 `gold` 標記寫入知識庫，執行前依 4.3 備份資料檔；有效預測須為 150 筆（見 4.2）。

## 4. 測試方法

### 4.1 測試環境需求

| 項目 | 規格 | 用於 |
|------|------|------|
| 硬體與作業系統 | 負責人個人電腦：Intel Core i7-13700HX（16 核 24 執行緒）、32 GB RAM、Windows 11 家用版 64 位元（build 26200）；後端、測試腳本與截圖擷取皆於此執行 | 全部 |
| 後端執行環境 | Python 3.12.9 虛擬環境 `code/backend/venv`；FastAPI＋Uvicorn 單 worker；依賴 `requirements.txt`；主控台 cp950（腳本輸出不含 emoji） | OP、FN、PF、QA |
| 前端建置環境 | Node.js 24（本機 v24.15.0）；Vite 8.3.0、React 19、Tailwind 4；`npm run build`、`npm run test:unit`（`node --test`） | QA-3、UI |
| 持續整合 | GitHub Actions `.github/workflows/ci.yml`（push／PR 至 `main` 觸發）：`test` job 為 ubuntu-latest、Python 3.12、`pytest tests -q`；`frontend` job 為 Node 20、`npm ci`、`npm run build`、`npm run test:unit` | FN 全部、QA-3 |
| 前端部署 | Vercel Hobby（team 與 project `fakenewsverify`，Root `code/frontend`）；`PUBLIC_BASE_URL` = `https://fakenewsverify.vercel.app`；`vercel.json` rewrites：`/api/*` 同源代理至 tunnel，其餘路徑 SPA fallback（排除 `*.html`） | OP-8、公開網址之檢查 |
| 對外網路 | Cloudflare quick tunnel（`cloudflared tunnel --url http://localhost:8000`，安裝與啟動見票 O-08）；網址 `*.trycloudflare.com` 重啟即變，依 `docs/rebuild/runbook_tunnel.md` 更新 `vercel.json` 並重新部署；單一請求須於 100 s 內完成 | OP-8 |
| AI 服務 | CGU AIR Gateway（OpenAI-compatible Responses API）：`AI_PROVIDER=cgu`、`CGU_MODEL=gpt-5.4-mini`、`OPENAI_REASONING_EFFORT=low`、embedding `text-embedding-3-small`（1536 維）；provider 鏈 `['cgu']`；額度 OpenAI 成本 USD 10＋本地模型 1,000 萬 tokens；用量 `GET {CGU_BASE_URL}/me/usage`；評測與量測一律 `USE_WEB_SEARCH=false`；無 OpenAI 直連，myai168 已停用 | OP-1、OP-2、PF-1、PF-2、PF-3a、PF-5、QA-1a、QA-1b |
| 介面測試工具 | Microsoft Edge 153 headless（Chrome DevTools Protocol）：桌機 1280 px、手機模擬 375 px 與 360×780、淺色與深色（模擬 `prefers-color-scheme`）；`VITE_FIXTURES=1` fixture 模式重現各狀態；`grep`、OpenCC；報告後另用 Lighthouse、axe DevTools | UI-1～UI-4、UI-6、UI-8；UI-5（報告後） |
| 行動裝置 | Android 手機與 iPhone 各一台（Android Chrome 110+、iOS Safari 16+，安裝 Threads App）：本輪供 OP-8 以行動網路開啟結果頁；報告後供真手機與 Threads App 內建瀏覽器測試 | OP-8；UI-7（報告後） |
| Threads | 本輪：`THREADS_MODE=sim`（`FakeThreadsService`；輸入 `data/threads_sim/mentions.json`、輸出 `data/threads_sim/replies.jsonl`；不需 Meta 憑證）。報告後：Meta 開發者帳號與 App（Threads use case；權限 `threads_basic`、`threads_content_publish`、`threads_manage_replies`、`threads_manage_mentions`）、公開之機器人帳號 `@factcheck_tw_bot` 與 Threads Tester 帳號、60 天長效 token | OP-3、FN-4、PF-3a；OP-4～OP-6、PF-3b、UI-7（報告後） |
| 測試資料 | `knowledge_base.parquet` 218 筆（D-05 清洗後種子，`verified=true` 128 筆，commit `db8c62d`）；`factcheck.db` 熱門 24 筆（全數 `verified=true`、無 Google News 轉址；不進 git，清洗前備份 `factcheck.db.bak-20260916`）；`eval_set.csv` 150 筆（SCAM／MISINFO／SAFE 各 50）；`docs/test/pf2_paraphrases.csv` 改寫謠言 20 筆（獨立於 eval_set）；OP-7 舊檔快照 `docs/test/op7_legacy/`；pytest fixture（離線，AI 與 Cofacts 一律 mock） | 全部 |
| 其他工具 | `curl`、`git`；ffmpeg 9.0.1 僅供 demo 影片剪輯（影片配音使用 CGU `gpt-4o-mini-tts`，費用計入 CGU 用量），不參與測試判定 | 4.4 影片交付 |

### 4.2 測試項目的通過準則

**單項判定**：各項目依第 3 節「通過準則」欄判定，所有可量測條件皆達標為「通過」，任一條件未達標為「未通過」。準則中標示為記錄項或「只記錄」之內容（OP-2 之 `--web-search`／`--allowed-domains` 兩次、FN-5 之實測長度規則紀錄欄、UI-7 之 App 內深連結）不影響判定。

**優先級清單**（spec §10「4.2 通過準則彙總」）：

- **P0**（30 項）：OP-1／2／3／7／8／9；FN-1／2a／3／4／5／6／7／8／9／11／12／13／14；UI-1／2／4／6／8；PF-1／2／3a；QA-1a／3／5。
- **P1**（16 項）：OP-4／5／6；FN-2b／2c／10；UI-3／5／7；PF-3b／4／5／6（可選）；QA-1b／2／4。

**本輪（報告前）整體通過條件**

| 條件 | 準則 |
|------|------|
| P0 | 全部 P0 項目通過（P0 項目均列為本次執行） |
| P1 | 本次執行之 P1 項目通過率 ≥80%；分母不含記錄性項目 QA-1b、PF-3b、PF-6（spec 明定其未達標只記錄、不擋交付） |
| 絕對閘門 | QA-1a：CGU `gpt-5.4-mini` 150 筆評測 FN=0，任一筆詐騙或假訊息判為 SAFE 即不通過；有效預測須為 150 筆（`eval_predictions.csv` 中 `errored` 為 0） |
| 回歸 | 宣告通過之 commit：`pytest tests` 全綠、`npm run build` 成功、GitHub Actions `ci` 兩個 job 皆綠 |
| 時點 | 2026-09-20 審查前達成（最早報告日 2026-09-21） |

**本輪與報告後之計算**

1. 標示為「報告後執行」之項目不列入本輪判定，於 6.1「報告後」欄計數；報告後輪次以相同準則判定並回填第 6 節（v1.1 起）。
2. 部分準則延後之本次執行項目（OP-3、FN-4、UI-1、UI-6、PF-3a；延後內容見第 3 節各表註）：本輪以未延後之準則判定，達標者記「通過（待補驗）」並計為通過；報告後補驗未達標時改記「未通過」並登錄 6.3。
3. 結果一律以下列用語標示：通過、未通過、通過（待補驗）、報告後執行、未執行（附原因）。

**準則修訂紀錄（v0.9）**：下列修訂於該項測試執行前經負責人核准，或屬事實更正；第 3 節修訂處以〔v0.9 修訂〕標示。

| ID | spec §10 原準則 | 修訂後 | 依據 |
|----|-----------------|--------|------|
| OP-1 | `stats.total ≥ 150` | `stats.total ≥ 125` | D-05 決定 3a：負責人於 apply 前核准接受清洗後已證實筆數低於原估 150（決定 2 刪除後約 125 筆）；未證實資料保留但不顯示、不參與向量命中（`docs/test/clean_sources_review.md`） |
| OP-9 | 知識庫 236 筆不變；`verified=true` ≥150 | 知識庫筆數僅減少核准刪除清單所列且存在於種子之筆數；`verified=true` ≥125 | D-05 決定 2a（核准刪除 27 筆髒資料，其中知識庫 19 筆）、決定 3a |
| FN-1 | 五個既有測試檔不得修改斷言 | `test_processor_flow.py`（B-03）與 `test_api.py::test_health_and_root`（B-22）兩處例外允許修改，其餘斷言不變 | 負責人同意閘門 O-01（`docs/rebuild/owner_decisions_day0.md`；`03_tickets.md` §0） |
| QA-1b | 舊數字日期記為 2026-07 | 2026-06（2026-06-06，commit `9d63eef`） | 事實更正（git 紀錄） |

### 4.3 測試中止的準則與再繼續的需求

**共通規則**（spec §10）：任一 P0 項目未通過即中止受影響之測試與程式凍結；再繼續須：缺陷修正、該項重跑通過、`pytest tests` 全綠且 `npm run build` 成功。會寫入資料之測試（QA-1a 之 `--seed-db`、PF-2、PF-3a、OP-1、OP-7、OP-9）執行前，先以複本備份 `knowledge_base.parquet`、`tasks.parquet`、`factcheck.db` 並記錄 SHA-256；同一時間只允許一個程序寫入（單寫者假設，spec §12 R10）。

| # | 中止準則 | 中止範圍 | 再繼續的需求 |
|---|----------|----------|--------------|
| 1 | 任一 P0 項目未通過（含 QA-1a 出現 FN>0） | 該項及以其結果為前提之項目（例：QA-1a 未通過時暫停 PF-5 成本結論與報告引用之評測數字）；暫停程式凍結與 tag | 缺陷登錄 6.3 並修正；該項重跑通過（QA-1a 須 150 筆整批重跑且 FN=0）；`pytest tests` 全綠、`npm run build` 成功 |
| 2 | CGU AIR 不可用：`test_ai_provider.py --provider cgu` 回 fallback 或 HTTP 401／429／5xx、`/health` 之 `ai_available=false`，或畫面出現「AI 分析暫時無法使用」 | 需呼叫 AI 之項目：OP-1、OP-2、PF-1、PF-2、PF-3a、PF-5、QA-1a、QA-1b；離線之功能測試與 fixture 截圖不受影響 | `test_ai_provider.py --provider cgu` 連續 2 次回合法 `risk_type` 且非 fallback；中斷期間之評測若有 API 錯誤筆（`errored`）則整批重跑（`--resume` 不會重試已記錄之錯誤筆）；中斷期間之量測樣本作廢重測 |
| 3 | CGU 用量超出預算：150 筆評測前後差額 > USD 1.5（PF-5 上限），或報告週累計 > USD 5（spec §9 成本） | 所有需呼叫 AI 之項目 | 石岱勳確認剩餘額度並核定調整後預算（記入 `docs/test/cgu_usage.txt`）；維持 `USE_WEB_SEARCH=false` |
| 4 | 資料檔損毀或遭覆寫：三個資料檔任一讀取失敗；或與測試前備份、`check_db.py` 基準（清洗後：知識庫 218 筆、`verified=true` 128 筆；熱門 24 筆全數 verified）之差異無法以該測試之預期寫入解釋；或發現兩個程序同時寫入 | 所有讀寫資料之項目（OP、PF、QA-1a、以真後端擷取之截圖） | 自備份還原：知識庫取測試前備份或已提交之清洗後種子（commit `db8c62d`）；熱門牆取測試前備份，或以 `factcheck.db.bak-20260916` 重跑 `clean_sources_2026_09.py --apply --drop-ids data/clean_sources_drop_ids.txt`（冪等）；`check_db.py` 輸出與基準一致或 SHA-256 與備份一致後，改為序列執行並重跑受影響項目 |
| 5 | CI 紅燈：GitHub Actions `ci` 之 `test` 或 `frontend` job 失敗 | 功能測試判定（3.2）、QA-3、程式凍結與正式部署 | 修正後 push，`ci` 兩個 job 皆綠；本機 `pytest tests -q` 全綠、`npm run build` 成功；修正不得違反 FN-1 之受保護斷言規則 |
| 6 | Tunnel 或公開部署中斷：`{PUBLIC_BASE_URL}/api/health` 非 200（含 Cloudflare 502／524），或 Vercel 部署失敗 | OP-8 與經公開網址之檢查；其餘項目改於本機（`localhost:8000`／`localhost:5173`）執行 | 依 `docs/rebuild/runbook_tunnel.md` 重啟 `cloudflared`、更新 `vercel.json` 主機並部署 Ready；`/api/health` 連續 3 次回 200 後重跑 OP-8 |
| 7 | 機密外洩：CGU 金鑰、`ADMIN_TOKEN`、Threads token 或 `.env` 內容出現在截圖、log 節錄、影片或 commit | 相關交付物之產出與公開 | 撤銷並更換外洩之金鑰或 token 使舊值失效；移除或遮蔽外洩素材；重新產出受影響之交付物並複查 |

### 4.4 測試交付項目

各項原始執行輸出（主控台輸出、API 回應、用量紀錄）存於 `docs/test/results/`，檔名以測試 ID 為前綴；6.2「證據檔案」欄引用該目錄之原始檔或下表之彙整檔。

| 交付項目 | 內容與路徑 | 對應測試 | 時點 |
|----------|------------|----------|------|
| 測試計畫書 | 本文件 `docs/test/TP-FNV-2026-01.md`（v1.0，第 6 節已填）與匯出檔 `docs/test/TP-FNV-2026-01_v1.0.pdf` | 全部 | 本輪 |
| 功能測試與 CI 紀錄 | GitHub Actions `ci` run 連結與截圖 `docs/test/ci_run.png`；本機 `pytest tests -q`、`npm run build`、`npm run test:unit` 輸出 | FN 全部、QA-3 | 本輪 |
| 操作測試紀錄 | `docs/test/op_log.md`（OP-1、OP-2、OP-3、OP-7、OP-8 之時間、指令與輸出節錄）；`docs/test/deploy_log.md`（部署網址、commit、時間） | OP | 本輪 |
| 評測報告 | 本輪：`code/backend/data/eval_report.csv`、`eval_binary.csv`、`eval_errors.csv`、`eval_predictions.csv`、`assets/confusion_matrix.png`；前一版封存：`code/backend/data/eval_archive_gpt5mini_2026-06/`；`docs/test/eval_summary.md`（新舊數字並列，標 provider／model／date） | QA-1a、QA-1b、QA-2、PF-1（未命中） | 本輪 |
| 績效紀錄 | `docs/test/perf_log.csv`（`result_id`、`origin`、`cache_layer`、`elapsed_ms`）、`docs/test/perf_summary.md`；`docs/test/threads_latency.csv` | PF-1、PF-2、PF-3a | 本輪（PF-3a live 部分報告後） |
| 成本紀錄 | `docs/test/cgu_usage.txt`（日期／動作／前／後／差） | OP-2、PF-5 | 本輪 |
| 介面截圖檢核 | `docs/test/ui_checklist.csv`；`docs/test/screens/`（檔名 `{畫面}_{狀態}_{寬度}_{主題}.png`，寬度 375 或 1280，主題 light 或 dark）；`docs/test/ui6_copy_check.txt` | UI-1～UI-4、UI-6、UI-8 | 本輪（S6 與真手機部分報告後） |
| 資料清洗證據 | `code/backend/data/clean_sources_report.txt`、`check_before_d05.txt`、`check_after_d05.txt`、`clean_sources_drop_ids.txt`；`docs/test/clean_sources_review.md` | OP-9、QA-5 | 已產出（2026-09-16） |
| Threads 證據 | 本輪：模擬模式 `replies.jsonl` 節錄與 `test_threads_bot.py --poll` 輸出；報告後：`docs/test/threads_live_log.md`（OP-5 原始 JSON 與錯誤 body） | OP-3、FN-4、PF-3a；OP-4～OP-6、PF-3b | 本輪／報告後 |
| 缺陷紀錄 | 本文件 6.3；修正 commit 與回歸測試（例：commit `c114413`、`tests/test_parquet_io.py`） | 全部 | 隨時登錄 |
| demo 影片與素材紀錄 | `docs/demo/demo_v0.3.0.mp4`（175.4 秒，不進 git，另附雲端連結）、`docs/demo/demo_v0.3.0.srt`；`docs/demo/footage_log.md`（錄製環境、各鏡 `result_id` 與 `cache_layer`、CGU 用量） | OP-3（模擬閉環呈現）；2.1 測試目標 1、2、4、6 | 已產出（2026-09-16） |
| 文件一致性勾核表 | `docs/test/doc_review.md` | QA-4 | 報告後 |

### 4.5 測試工作項目與時程

**本輪（報告前）**：以最早報告日 2026-09-21 為截止（`owner_decisions_day0.md`）。日期為預定，實際執行日期記於第 6 節；會寫入資料之項目依表列順序序列執行（4.3）。

| 日期（2026） | 工作項目 | 測試 ID | 主責 | 產出／完成條件 |
|--------------|----------|---------|------|----------------|
| 09-15（二） | 規格書 v1.3、實作票、計畫書第 1–3 節初稿（v0.1）；清洗腳本 dry-run 與審閱文件 | —（準備） | 石岱勳；Claude Code | 已完成：`01_spec.md`、`03_tickets.md`、本計畫 v0.1、`clean_sources_review.md` |
| 09-16（三）凌晨 | D-05 清洗套用與二次 apply（冪等）；以模擬模式錄製 demo 影片 v0.3.0；修正結果頁偶發 500（DEF-01） | OP-9、QA-5；OP-3（模擬部分） | 石岱勳；Claude Code | 已完成：`check_after_d05.txt`、`footage_log.md`、commit `c114413` |
| 09-16（三） | 計畫書 v0.9；補齊測試前置（B-02 `test_ai_provider.py` 旗標、B-23 `evaluate.py --timing` 與報告欄位、O-11 改寫謠言 20 筆）；確認本機 pytest、前端 build 與 CI；CGU provider 測試與用量基準 | FN（本次執行項目）、QA-3、OP-2 | 石岱勳；張宇宏（審定改寫謠言）；Claude Code | 本計畫 v0.9；CI 綠；`cgu_usage.txt` 基準列 |
| 09-16（三）～09-17（四） | 資料檔備份後依序執行：150 筆評測（`USE_WEB_SEARCH=false`，前後查用量）→ 快取命中延遲與向量命中率 → 模擬模式 Threads 延遲 | QA-1a、QA-1b、QA-2、PF-5、PF-1、PF-2、PF-3a（模擬部分） | 張宇宏；Claude Code | `eval_*.csv`、`eval_summary.md`、`perf_log.csv`、`perf_summary.md`、`threads_latency.csv` |
| 09-17（四） | 介面截圖擷取（Edge headless）、逐格勾核與文案／顏色／來源紀律檢查 | UI-1、UI-2、UI-3、UI-4、UI-6、UI-8 | 姚睿（勾核）；Claude Code（擷取與自動檢查）；廖昱翔（覆核） | `ui_checklist.csv`、`screens/`、`ui6_copy_check.txt` |
| 09-17（四）～09-18（五） | demo 影片審片（查核來源皆 Tier 1／2、無金鑰入鏡、無 fallback 畫面、長度 ≤3:00）；簡報數字依實測紀錄更新 | —（交付項目） | 廖晢勛 | 審片結果記入 `footage_log.md` |
| 09-18（五） | 啟動 tunnel、更新 `vercel.json` 並確認部署；發佈前操作測試：全新 clone、舊檔載入、公開網址（含手機行動網路） | OP-8、OP-1、OP-7 | 石岱勳；姚睿（手機） | `op_log.md`、`deploy_log.md` |
| 09-19（六） | 缺陷修正與回歸；未通過項目依 4.3 重測；彙整 6.1～6.3 | 依 6.3 | 石岱勳；Claude Code；廖昱翔 | 回歸後 CI 綠；6.1～6.3 填妥 |
| 09-20（日） | 第 6 節審查、升 v1.0 並匯出 PDF；demo 彩排（模擬模式）；程式凍結（tag `v0.3.0-demo`） | 本輪全部 | 廖昱翔（審查）；廖晢勛（彩排）；石岱勳（凍結）；指導教授簽核依其時程 | 本計畫 v1.0、`TP-FNV-2026-01_v1.0.pdf`、tag |
| 09-21（一）～09-23（三） | 進度報告與 demo 影片發表（確切日期依課程公告）；待命處理阻塞缺陷（只修 demo 阻塞者） | — | 全員 | 報告完成 |

**報告後**：目標日期為暫定，依課程進度與 Meta App 設定進度調整；完成後回填第 6 節（v1.1 起）。

| 目標日期（2026） | 工作項目 | 測試 ID | 前置條件（票） |
|------------------|----------|---------|----------------|
| 09-24（四）～09-25（五） | 文件同步與一致性勾核 | QA-4 | B-25 |
| 09-24（四）～09-30（三） | Meta App 與 Threads use case 設定、Tester 邀請與接受（帳號公開）、取得長效 token；live 探測；兩段式發佈、錯誤分流與 backoff；live 端到端回覆與延遲量測 | OP-4、OP-5、PF-3a（live 部分）、PF-3b；FN-4 延後案例、FN-5 紀錄欄 | O-07、O-09、T-12～T-17 |
| token 取得滿 24 小時後（10-01 前） | token 續期 | OP-6 | OP-4 通過 |
| 10-01（四）～10-03（六） | 機器人狀態頁完成後補驗 | OP-3、UI-1（S6）、UI-6（S6 文案）之延後部分 | S-11 |
| 10-05（一）～10-09（五） | 真手機（iOS、Android）與 Threads App 內建瀏覽器測試；無障礙量測（Lighthouse、axe、鍵盤操作） | UI-7、UI-5 | P-18、O-28；Threads 實機串接完成 |
| 10-05（一）～10-16（五） | P1 功能實作後之測試：圖片查證、`similar_news` 向量近鄰、AI 額度護欄 | FN-2b、FN-2c、FN-10 | B-26／S-13；D-18；FR-14 程式 |
| 10-12（一）～10-16（五） | 模擬模式 10 則一輪量測；`gpt-5.4` 模型比較組（可選） | PF-4、PF-6 | 10 則 mention fixture；`/me/usage` 餘額 ≥ USD 4 |
| 10-19（一） | 報告後輪次結果回填第 6 節並審查 | 上列全部 | 上列完成 |

## 5. 人員配置與訓練需求

### 5.1 測試人員配置

團隊共 5 人（共識 §1）。姓名與分工於 2026-09-16 由負責人定案（票 O-10 結案），下表以姓名標示。「代號」欄僅供對照本輪之前已產出、沿用組員 A～E 代號之檢核表與執行紀錄（`docs/test/ui_checklist.csv`、`docs/test/results/*`）；該組代號與規格書 §3、§11 之代號不同。指導教授姓名待填。

**AI 輔助聲明**：本專題的程式實作與自動化測試執行，包括撰寫程式與測試碼、執行 `pytest`、評測與量測腳本、以 headless 瀏覽器擷取截圖、彙整紀錄檔，由 AI 程式開發助理 Claude Code（Anthropic）協助完成，全程在石岱勳（負責人）監督下進行。負責人決定範圍與準則、審核每一項程式變更與測試結果，並負責金鑰與資料之存取；需要人工判斷的工作（介面勾核、資料清洗審閱、測試結論與簽核）由組員與審查者執行，不由 AI 代為判定。

| 姓名 | 代號（既有紀錄對照） | 角色 | 職責 | 負責測試項目 |
|------|------|------|------|--------------|
| 石岱勳（負責人） | 組員 A | 測試負責人／操作者 | 訂定與維護測試計畫與時程；管理測試環境（`.env` 金鑰、CGU 用量、tunnel、Vercel）；執行操作測試；判定中止與再繼續（4.3）；監督 Claude Code 之實作與自動化執行並審核結果；報告後擔任 Threads Tester（發布測試貼文） | OP-1～OP-9、QA-3、QA-5；FN 全部（監督執行） |
| 姚睿 | 組員 B | 介面測試員（真手機） | 截圖檢核表逐格勾核（非實作者）；以手機行動網路開啟公開網址；報告後以 iOS／Android 真手機與 Threads App 內建瀏覽器測試；報告後擔任 Threads Tester（回覆並 @機器人） | UI-1～UI-4、UI-6、UI-8、OP-8（手機部分）；報告後 UI-5、UI-7、PF-3b |
| 張宇宏 | 組員 C | 績效與品質數據 | 審定 PF-2 改寫謠言 20 筆；監看評測執行並記錄 CGU 用量；彙整延遲、命中率與成本；撰寫評測錯誤分析 | PF-1～PF-6、QA-1a、QA-1b、QA-2 |
| 廖昱翔 | 組員 D | 文件與審查 | 維護本計畫書並彙整第 6 節；檢查交付項目完整性；覆核截圖檢核表；協助清洗審閱抽查；文件一致性勾核 | QA-4、QA-5（協助）；4.4、6.1～6.3 |
| 廖晢勛 | 組員 E | 影片與簡報 | demo 影片審片（對照 `footage_log.md`）；彩排；進度報告簡報；報告後補錄 live 片段 | 4.4 影片交付；OP-3（閉環呈現） |
| 指導教授（姓名待填） | — | 審查者 | 審閱本計畫與第 6 節結果並簽核 | 6.4 |
| Claude Code（AI 輔助工具，非組員） | — | 實作與自動化執行 | 於石岱勳監督下撰寫程式與測試、執行自動化測試與量測腳本、擷取截圖、彙整紀錄；結果一律經石岱勳確認 | FN 全部、PF／QA 腳本、UI 截圖擷取（僅執行，不作判定） |

### 5.2 訓練需求

| 訓練項目 | 對象 | 內容 | 時點 | 完成確認 |
|----------|------|------|------|----------|
| 系統啟動與測試腳本 | 石岱勳講授；姚睿、張宇宏、廖昱翔、廖晢勛 | `start.bat` 一鍵啟動（自動產生 `ADMIN_TOKEN`；`.env` 不得外流）；`pytest tests -q`、`npm run build`、`npm run test:unit`；`test_ai_provider.py --provider cgu`；`test_threads_bot.py --reset-sim`／`--poll`（模擬模式）；`check_db.py`；Windows 主控台 cp950 輸出限制 | 09-16 | 每人至少於一台電腦完成一次 `pytest` 全綠 |
| CI 判讀 | 石岱勳、廖昱翔 | GitHub Actions `ci` 之 `test`、`frontend` 兩個 job；失敗 log 定位；與本機結果比對；CI 紅燈時依 4.3 中止 | 09-16 | 能說明最新一次 run 之狀態並附連結 |
| 評測腳本使用 | 張宇宏 | `evaluate.py` 參數（`--seed-db`、`--delay`、`--resume`、`--report-only`）與輸出檔；`USE_WEB_SEARCH=false`；前後查 `/me/usage` 並記錄；`errored` 筆數與 FN／FP 判讀；單寫者限制與執行前備份 | 09-16 | 能依 `eval_binary.csv`、`eval_errors.csv` 說明 FN、FP 與錯誤筆之意義 |
| 截圖檢核表 | 姚睿（廖昱翔覆核） | `ui_checklist.csv` 欄位與檔名規則；spec §8.4 狀態總表；§8.7 文案逐字比對；§8.6 違規（字級 <13 px、hover-only 說明）；來源 tier 小標與紅黃綠使用紀律；不通過時登錄缺陷 | 09-17 擷取前 | 試勾 3 列並經石岱勳確認 |
| AI 輔助產出審核 | 石岱勳 | 以 `git diff` 檢視 Claude Code 之變更；本機重跑 `pytest` 並比對 CI 以獨立確認結果；確認受保護測試斷言未被修改（FN-1）；確認第 6 節數字出自原始紀錄檔 | 持續 | 第 6 節每筆結果皆附證據檔案 |
| Threads Tester 設定 | 石岱勳、姚睿（指導教授可選） | Meta App 角色邀請與於 Threads App「網站權限」接受；帳號設為公開；開發模式限制（只讀得到 Tester 之提及與貼文）；回覆可疑貼文並 @`factcheck_tw_bot` 之操作 | 報告後（09-24 起） | 後台角色頁截圖與帳號公開確認 |
| 真手機測試程序 | 姚睿 | iOS Safari、Android Chrome 與 Threads App 內建瀏覽器開啟 `/r/{id}`；分享 Web Intent 驗證；結果頁 3 秒可讀性判定方式 | 報告後（10-05 前） | 完成 1 筆試測紀錄 |

## 6. 測試結果與審查

本節依 2026-09-16 之實測紀錄填寫。結果用語依 4.2：通過、通過（待補驗）、未通過、未執行（附原因）、報告後執行；每一筆數字均出自 4.4 所列之原始紀錄檔（`docs/test/results/`），凡由本節換算者於「主要實測數據」欄註明。

### 6.1 測試執行摘要

| 項目 | 內容 |
|------|------|
| 執行期間 | 2026-09-16（+08:00）：功能（3.2）與品質（3.5）03:11–03:47；操作（3.1）、介面（3.3）與績效（3.4）03:07–04:12；缺陷 DEF-02 之修正與回歸至 04:26。兩組測試並行執行並共用同一把 CGU 金鑰（見下方環境差異第 6 點） |
| 受測版本（commit） | 量測起始 `c114413`（= 當時 `origin/main`，工作樹乾淨）；量測期間合入 `b1fc5c3`（`parquet_io` 重試視窗加長、`evaluate.py` 於 0 筆錯誤時清空 `eval_errors.csv`）；本節宣告版本為 **`ad13f2f`**（= `origin/main`，含 DEF-02 修正與其回歸測試，後端 `pytest tests` **478 passed**）。8030 後端量得之操作／介面／績效數據執行的是 `c114413` 之程式碼 |
| CI run | 宣告版本 `ad13f2f`：run **35019722949**，`test` 與 `frontend` 兩個 job 皆 success；`b1fc5c3`：run 35015937421 success；量測當時最新為 `c114413` 之 run 35008720030（`test` job 19 s）。自 2026-09-14 重做起算之 17 次 run 全數 success、0 次失敗 |
| 與 4.1 環境之差異 | 1. 後端跑在 `127.0.0.1:8030`（OP-7 另開 8031 指向舊資料複本）而非 8000；前端以暫存 Vite dev server（5310／5311／5330／5342，獨立設定與 cacheDir）而非 5173，以免動到測試負責人自用的 5173／5179／5199。2. 未啟動 Cloudflare quick tunnel、未經 Vercel 公開網址測試（OP-8 未執行；`vercel.json` 之 tunnel 主機仍為 `REPLACE-ME.trycloudflare.com`）。3. 推理強度實際為 `CGU_REASONING_EFFORT=medium`（4.1 與 spec §9 寫的是 `OPENAI_REASONING_EFFORT=low`，provider `cgu` 讀的是前者；鍵名不一致列入 QA-4 文件同步）。4. 測試前置票 B-02（`test_ai_provider.py --web-search`／`--allowed-domains`）與 B-23（`evaluate.py --timing` 與報告欄位）未合入 HEAD，OP-2 記錄項與 PF-1 未命中子準則因此無法依計畫方法執行。5. 本機未安裝 OpenCC、Lighthouse、axe，且本輪不新增安裝（UI-6 簡繁 diff、UI-5 量測未執行）。6. 兩組測試並行，延遲數據是在閘道同時承受 150 筆評測流量下量得；`/me/usage` 差額混有兩組流量，成本以閘道計價分攤估算。7. QA-1a 未加 `--seed-db`（避免與並行測試同時寫入資料檔，單寫者假設），只影響判對案例是否回寫知識庫，不影響預測與指標。8. 介面檢核表之「目視」欄為 Claude Code 預檢，未經非實作組員勾核與覆核（UI-1 因此未執行）。9. PF-2 的 20 筆改寫句由 Claude Code 為本次量測撰寫，非票 O-11 所指之組員審定版本 |

| 類別 | 項目數 | 本次執行（§3 指定） | 通過 | 通過（待補驗） | 未通過 | 未執行 | 報告後執行 |
|------|--------|----------------------|------|----------------|--------|--------|------------|
| 操作測試（3.1） | 9 | 6 | 3 | 1 | 0 | 1 | 4 |
| 功能測試（3.2） | 16 | 13 | 12 | 0 | 1 | 0 | 3 |
| 介面測試（3.3） | 8 | 6 | 4 | 0 | 0 | 2 | 2 |
| 績效測試（3.4） | 7 | 4 | 1 | 1 | 1 | 1 | 3 |
| 品質測試（3.5） | 6 | 5 | 4 | 0 | 1 | 0 | 1 |
| **合計** | **46** | **34** | **24** | **2** | **3** | **4** | **13** |
| **其中 P0 小計** | **30** | **30** | **21** | **2** | **2** | **4** | **1** |

註：

1. 右側五個結果欄之和 = 項目數。「本次執行（§3 指定）」為第 3 節「本次執行」欄所列項目數，與結果欄的「報告後執行」相差 1 項，差額為 OP-8（見註 2）。
2. **OP-8（P0）**在第 3 節列為本次執行、4.5 排 09-18，本輪因未啟動 tunnel 與公開部署而未執行，改列報告後執行；4.2「P0 項目均列為本次執行」之前提本輪因此不成立，此範圍變更須經 6.4 審查確認。
3. 依 4.2 第 2 點，「通過（待補驗）」計為通過：OP-3（待補 `/bot` 頁顯示）、PF-3a（待補 live 3 次）。
4. FN-2b、FN-2c、FN-10、OP-4～OP-6、UI-5、UI-7、PF-3b、PF-4、PF-6、QA-4 於第 3 節即列為報告後執行，依 4.2 第 1 點不列入本輪判定。功能／品質執行結果檔將 FN-2b／2c／10 記為「未執行（功能未實作、無測試）」，本節依 4.2 統一記為「報告後執行」，兩者所指狀態相同。
5. 未執行 4 項（OP-1、UI-1、UI-6、PF-1）之原因逐項見 6.2，均為「需另備環境、人員勾核或前置票未合入」，非測試失敗。

| 整體判定（4.2） | 結果 |
|------------------|------|
| P0 通過數／P0 項目數 | **23／30**（通過 21＋通過（待補驗）2）。未通過 2（FN-4、PF-2）、未執行 4（OP-1、UI-1、UI-6、PF-1）、報告後執行 1（OP-8）→ **未達「全部 P0 通過」** |
| 本次執行之 P1 通過率（不含記錄性項目） | 本次執行之 P1 共 4 項（UI-3、PF-5、QA-1b、QA-2），依 4.2 扣除記錄性項目 QA-1b 後分母為 3：通過 2（UI-3、PF-5）、未通過 1（QA-2）＝ **66.7%**，**未達 ≥80%** |
| QA-1a FN（有效預測筆數） | **FN = 0**；有效預測 **150／150**（`eval_predictions.csv` 之 `errored` 為 0），TP 100／FP 0／TN 50 → **絕對閘門達成**（執行偏差：未加 `--seed-db`，見 6.2） |
| 通過（待補驗）之項目 | **OP-3**（待補驗：`/bot` 頁 ≤4 秒顯示該回覆，票 S-11）、**PF-3a**（待補驗：live 3 次量測） |

**本輪是否達到 4.2 之通過準則：否。** 四項本輪整體通過條件中，只有「絕對閘門 QA-1a（FN=0）」與「回歸（宣告版本 `ad13f2f` 之 `pytest` 全綠、`npm run build` 成功、CI 兩個 job 皆綠）」達成；「全部 P0 通過」與「本次執行之 P1 通過率 ≥80%」兩項未達成，時點條件（09-20 審查前）尚在期限內。未達成之具體內容：

1. **FN-4（P0，未通過）**：Threads 機器人之三項實機發佈與錯誤分流行為——HTTP 429 → 設定 `backoff_until`、未知 4xx → 標 failed 不回覆、container `FINISHED` 才 publish 且 `ERROR` 重試一次——**尚未實作**，因此不適用「通過（待補驗）」，依 4.2 判為未通過；模擬模式其餘 7 項子準則之 73 個測試全數通過。修正依票 T-13（前兩項）與 T-12（第三項），目標 **2026-09-24～09-30**，並須一併統一 spec §7.5（其他 4xx → unreadable）與 §7.10／FN-4（未知 4xx → failed）之文字衝突。
2. **PF-2（P0，未通過）**：向量快取命中率 65%（13／20）低於 ≥70%，差 1 筆。原因分析與改善計畫見 6.2 該列與 6.3（DEF-05）；目標 **報告後（10-05 前）** 以組員審定之改寫句題組重測。
3. **未執行之 4 項 P0**：OP-1（全新 clone）、UI-1（深色全套與非實作組員勾核）、UI-6（OpenCC 與英文字允許清單界定）、PF-1（未命中延遲需 `--timing`，票 B-23）；OP-8 改列報告後。若要在報告前補齊，依 4.5 須於 09-17～09-20 完成。

**對進度報告的意義**：本計畫書本輪**不得宣稱「全部 P0 通過」**。進度報告與 demo 影片可引用的已驗證數字，限於本節判為通過之項目——150 筆評測 accuracy 1.000／FN 0／FP 0（QA-1a、QA-1b）、L1 快取命中 p50 213 ms 與 L2 向量命中 p50 3.06 s（PF-1 之子準則）、模擬模式 Threads 閉環回覆 205–264 ms（OP-3、PF-3a）、來源紀律與清洗後資料（UI-8、OP-9、QA-5）、CI 全綠與後端 478 個測試（QA-3）——並須同時說明未達標項目與其時程。依 4.3 第 1 列，FN-4 與 PF-2 觸發中止準則：受影響範圍為「Threads 實機發佈行為」與「向量快取命中率」兩項對外宣稱，其餘已通過項目之結論不受影響；程式凍結與 tag `v0.3.0-demo` 依 4.5 於 09-20 前，視 OP-1、OP-8、UI-1、UI-6、PF-1 之補齊情形由測試負責人石岱勳決定。

### 6.2 各項結果明細

「本次執行」欄同第 3 節，報告後執行之原因見第 3 節各表；OP-3、FN-4、UI-1、UI-6、PF-3a 有部分準則延後，結果依 4.2 標示。證據路徑以 `docs/test/results/` 為根（`raw/…` 即 `docs/test/results/raw/…`；`functional_quality`、`operation_ui_performance` 指同目錄之 `functional_quality_2026-09-16.md`、`operation_ui_performance_2026-09-16.md`），其餘路徑自專案根目錄起算。

| ID | 優先級 | 本次執行 | 結果 | 主要實測數據 | 證據檔案 |
|----|--------|----------|------|--------------|----------|
| OP-1 | P0 | 本次執行 | 未執行（附原因） | **未執行原因**：須於另一資料夾全新 clone 並由 pip／npm 下載套件、金鑰由石岱勳自填（3.1 註 3），本輪執行者不下載套件、不處理金鑰；4.5 排 09-18。以本機 8030 預檢可判定之子項皆達標：`/health` 與 `/api/health` 200、`ai_available=true`、`threads_mode=sim`；`GET /api/knowledge/stats.total = 128`（門檻 ≥125）、`unverified_count=90`；`POST /api/threads/poll` 無 token／錯 token 皆 401、正確 token 202；`/api/result/{不存在}` 404；後端啟動至 `/health` 200 為 1.89 s。未驗：<5 分鐘（含 pip／npm）、`.env` 自動產生 ≥32 字 `ADMIN_TOKEN`、前端 5173 | `raw/op_startup_health_8030.txt`、`raw/op_backend_runtime_notes.txt` |
| OP-2 | P0 | 本次執行 | 通過 | P0 條件全數達標：provider 鏈 `['cgu']`、`risk_type=SAFE`（合法值）、非 fallback、`usage={input_tokens: 1259, output_tokens: 304}`、`model=gpt-5.4-mini`、embedding 維度 **1536**；exit 0、約 8.4 s、依閘道計價約 USD 0.0046。記錄項未執行（腳本無 `--web-search`／`--allowed-domains`，票 B-02 未合入；`docs/test/cgu_usage.txt` 未建立），依 4.2 記錄項不影響判定 | `raw/op2_test_ai_provider_cgu.txt` |
| OP-3 | P0 | 本次執行 | 通過（待補驗） | 3 輪 `--reset-sim` → `--poll`：每輪 `POST /api/threads/poll` 202、`checked=3 replied=3 skipped=0 errors=0`、`replies.jsonl` 每輪新增 3 行、`status.mode == "sim"`；m1（文字）走 L1 hash 命中並回覆紅燈「詐騙警告」＋來源 mygopen.com＋`{PUBLIC_BASE_URL}/r/{id}`。待補驗：`/bot` 頁 ≤4 秒顯示該回覆（票 S-11，報告後；目前為佔位頁） | `raw/pf3a_threads_sim_rounds.txt`、`raw/pf_backend_structured_log_8030.txt`；`docs/test/screens/S6_placeholder_375_light.png` |
| OP-4 | P1 | 報告後執行 | 報告後執行 | 需 Meta App、Threads Tester 與實機授權；4.5 排 09-24～09-30 | — |
| OP-5 | P1 | 報告後執行 | 報告後執行 | 需 Threads 實機 token 與 Tester 帳號；FN-5 之「實測長度規則紀錄欄」依附本項 | — |
| OP-6 | P1 | 報告後執行 | 報告後執行 | 需實機 token 取得滿 24 小時（OP-4 通過後、10-01 前） | — |
| OP-7 | P0 | 本次執行 | 通過 | 舊檔快照 SHA-256 與 `op7_legacy/README.txt` 三檔一致；以工作樹 HEAD 程式在 8031 啟動、2.17 s 健康。**載入成功**：`/health`、知識庫、熱門、`/api/threads/status` 與舊任務 completed×2／pending×2／failed×2 的 `/api/result/{id}` 皆 200。**新欄位為預設值**：知識庫 `verified` 補 False（stats `total=0`、`unverified_count=236`）、舊任務 `verification_status=unverified`、`source_tier=null`、`similar_news=[]`；`factcheck.db` 啟動時 `ALTER` 新增 6 欄。**筆數不變**：`check_db.py` 前後輸出逐字相同（知識庫 236、熱門 63），`tasks.parquet` 33 筆。差異：以 scratchpad 複本代替全新 clone（程式同為工作樹 HEAD）。附帶發現 DEF-02 | `raw/op7_check_db_before.txt`、`raw/op7_check_db_after.txt`、`raw/op7_legacy_api_checks.txt`、`raw/op7_legacy_results.txt` |
| OP-8 | P0 | 本次執行 | 報告後執行（本輪範圍變更，見 6.1 註 2） | 本輪未執行：需 Cloudflare quick tunnel＋Vercel 公開部署與手機行動網路；`vercel.json` 之 tunnel 主機仍為 `REPLACE-ME.trycloudflare.com`。4.5 原排 09-18，本輪改列報告後，須經 6.4 確認 | `operation_ui_performance` §2 |
| OP-9 | P0 | 本次執行 | 通過 | ① dry-run 不改檔（以提交種子之複本重跑，兩檔 SHA-256 與 mtime 皆不變）；② 各規則影響筆數與逐筆 before→after 見 D-05 dry-run 報告與審閱紀錄；③ apply 後知識庫 **218 = 236 − 18**（核准刪除清單中存在於種子者）、`verified=true` **128**（≥125）、熱門牆 24 筆、`news.google.com` **0**；④ `knowledge_base.parquet.bak-20260916`、`factcheck.db.bak-20260916` 存在；⑤ 第二次 apply 各規則影響 **0**（冪等，Cofacts 網路查詢 0 次）；⑥ `pytest tests` 綠 | `docs/test/clean_sources_review.md`；`code/backend/data/clean_sources_report.txt`、`check_before_d05.txt`、`check_after_d05.txt`；`raw/op9_idempotency_rerun_scratch.txt` |
| FN-1 | P0 | 本次執行 | 通過 | 8 檔 **37 passed**。`test_cache_and_store`／`test_marking_rules`／`test_url_validator` 自 2026-07-12 起未改；`test_processor_flow.py`（B-03）與 `test_api.py::test_health_and_root`（B-22）之斷言修改為 4.2 已登記之例外。新增測試：文字輸入不呼叫 crawler（`CountingCrawler.calls == 0`）、系統 prompt 含 FR-19 逐字規則之字串比對，皆通過。備註：spec §10.2 FN-1 原文尚未回寫這兩項例外（列入 6.4 待確認事項） | `raw/fn_per_item_pytest.txt`、`raw/fn1_protected_tests_git_history.txt` |
| FN-2a | P0 | 本次執行 | 通過 | **53 passed**：10 個私網／協定樣本全部拒絕且未發出請求、3 個公網放行；連線逾時 10 s 與 `CRAWLER_TIMEOUT` 以 mock 各自驗證；爬取失敗 5 種錯誤碼回 UNVERIFIABLE 且 AI 0 次；非網址 422；同網域來源剔除。補充端點實測：10 樣本 × 2 端點共 19 次 422（`blocked_url` 18、`invalid_url` 1——`ftp://` 在 `/api/analyze/url` 回 `invalid_url`，在 `/sync` 走文字管線），對外請求 0 次 | `raw/fn_per_item_pytest.txt`、`raw/fn2a_adhoc_endpoint_all10.txt` |
| FN-2c | P1 | 報告後執行 | 報告後執行 | `similar_news` 向量近鄰未實作（`pandas_task_processor.py` L215、L479 固定回 `[]`，D-18），無對應測試 | `raw/fn_per_item_pytest.txt` |
| FN-2b | P1 | 報告後執行 | 報告後執行 | 圖片查證端點未實作 magic bytes／10 MB 檢查（票 B-26 報告後），無 415／413／bytes hash 測試 | `raw/fn_per_item_pytest.txt` |
| FN-3 | P0 | 本次執行 | 通過 | **38 passed**：`/api/result/{id}` 對 pending／completed／failed／404／ai_unavailable 五態 schema 正確；sync／async／threads_sim 三種來源皆 200；`/health` 含 `scheduler.{enabled,interval_hours}`。DEF-02 修正後另增回歸測試 `test_legacy_fallback_task_marked_ai_unavailable` | `raw/fn_per_item_pytest.txt`；`code/backend/tests/test_result_api.py` |
| FN-4 | P0 | 本次執行 | **未通過** | **73 passed**，但 10 項子準則中僅 7 項有測試且通過（文字 mention `threads_len ≤ 400` 且含 `/r/`、IMAGE → `reply_media_only`、`get_post` 403 → `reply_cannot_read`、二次 poll 不重複、fallback 不回覆不標記、自我貼文兩種跳過、每則後 state 已寫入）。**3 項行為尚未實作、亦無測試**：① HTTP 429 → 設定 `backoff_until`（現行把 429 當 transient、下輪重試）；② 未知 4xx → 標 failed 不回覆（現行依 spec §7.5 回 `reply_cannot_read` 並標記，與 §7.10／本準則原文衝突）；③ container `FINISHED` 才 publish、`ERROR` 重試一次（現行建 container 後直接 publish，無狀態輪詢）。因屬**未實作**而非「已實作但本週無法驗證」，依 4.2 判未通過（不適用「通過（待補驗）」）。修正：票 T-13（①②）、T-12（③），目標 **09-24～09-30**；併同統一 spec §7.5 與 §7.10 之衝突 | `raw/fn_per_item_pytest.txt`；`code/backend/tests/test_threads_bot_sim.py` L639-667；`app/workers/threads_bot.py` L185-187；`app/services/threads_service.py` L299-311 |
| FN-5 | P0 | 本次執行 | 通過 | **21 passed**：七種第一行模板 `threads_len ≤ 400`（目標常數 `THREADS_TARGET_LEN = 400`）、連結數 ≤2；4 emoji＋200 字與含警示符號（U+26A0 U+FE0F）兩個極端案例 ≤500 且保留結果頁連結；「尚無查核機構證實」替代行之連結數 = 1；`threads_len` 對 BMP emoji／U+FE0F／U+200D 以 bytes 計。記錄項「實測長度規則紀錄欄」依附 OP-5（報告後），依 4.2 不影響判定 | `raw/fn_per_item_pytest.txt` |
| FN-6 | P0 | 本次執行 | 通過 | **23 passed**：`frame_of()` 對 spec §7.8 表 **8 列** + 13 組額外輸入（準則要求 ≥11）；含「SAFE、0.95、unverified → yellow 尚無查核機構證實」、「SAFE、0.95、verified → green」、「缺 `verification_status` 鍵 → 視為 unverified」三組 | `raw/fn_per_item_pytest.txt` |
| FN-7 | P0 | 本次執行 | 通過 | **8 passed**：fallback 同時帶 `ai_unavailable=True` 與 summary 前綴；`scripts/evaluate.py`、`scripts/test_ai_provider.py` 皆改呼叫 `verdict.is_fallback`（單一判斷點） | `raw/fn_per_item_pytest.txt` |
| FN-8 | P0 | 本次執行 | 通過 | **8 passed**：`q="("` 200；`offset` 分頁不重複不遺漏；stats 四格加總 = total；fixture 中 `verified=false` 列不出現在列表且不計入 `total` | `raw/fn_per_item_pytest.txt` |
| FN-9 | P0 | 本次執行 | 通過 | **10 passed**：無 token／錯 token → 401；`ADMIN_TOKEN` 空或空白 → 403；正確 → 200（trending refresh）與 200／202／409（threads poll）；比對使用 `hmac.compare_digest` | `raw/fn_per_item_pytest.txt` |
| FN-10 | P1 | 報告後執行 | 報告後執行 | `DAILY_AI_CALL_CAP` 未實作（`app/` 與 `tests/` 皆無此字串），FR-14 護欄程式排報告後 | `raw/fn_per_item_pytest.txt` |
| FN-11 | P0 | 本次執行 | 通過 | **29 passed**：同文 hash 命中；改寫文以固定向量 fixture 走完整管線得 `cache_layer == "vector"` 且 AI 0 次；`verified=false` 列即使相似度 1.0 仍不命中 vector 層、hash 層仍命中 | `raw/fn_per_item_pytest.txt` |
| FN-12 | P0 | 本次執行 | 通過 | **32 passed**：`test_tier_table` 20 組（≥15），含 `gov.tw.evil.com` → 3、`news.google.com/rss` → 3、MyGoPen 任意標題 → 1、ETtoday 查核標題 → 2／一般標題 → 3、offline Cofacts → 3 且未發請求；Cofacts 有回覆 → 1／無回覆 → 3／GraphQL 逾時 → 3；GraphQL 端點斷言 `https://api.cofacts.tw/graphql`；Tier 2 以 mock 斷言委派 `news_fetcher._title_indicates_debunk` | `raw/fn_per_item_pytest.txt` |
| FN-13 | P0 | 本次執行 | 通過 | **22 passed**：FR-17 驗收 1 之四組（Tier 1 → true；只有 Tier 3 → false 且 vector 不命中；`sources=[]` + rule → true；Tier 1 但 validator 判死 → false）；`/api/result` 之 `verification_status` 三值正確；回應 `sources[]` 無 tier 3 | `raw/fn_per_item_pytest.txt` |
| FN-14 | P0 | 本次執行 | 通過 | **11 passed**：fixture Parquet 與 SQLite 在 `--dry-run` 後 mtime 與 sha 不變；`--apply` 兩次結果相同且有備份；Google News 解析成功（覆寫＋去重）與失敗（刪列）兩種；`label_source="rule"` 列永不降級 | `raw/fn_per_item_pytest.txt` |
| UI-1 | P0 | 本次執行 | 未執行（附原因） | **未執行原因**：本輪只擷取淺色全套＋S2 深色抽查，**深色全套未擷取**；且準則要求由非實作組員（姚睿）勾核、文件審查（廖昱翔）覆核，本輪檢核表「目視」欄為 Claude Code 預檢，不代替勾核者。S6 `/bot` 格依票 S-11 延後、S4「載入更多」因範圍刪減不適用。4.5 排 09-17。已擷取之 **84 列全部「通過」、0 列未通過**：依 8.4 母版逐格擷取 S1–S4 各狀態淺色 74 張＋S2 深色 7 張，皆符合 §8.7 文案且無 §8.6 違規；81 張截圖之 DOM 自動檢查：橫向溢出 0、每頁 h1 恰 1 個、<13 px 可見文字 0、導覽／按鈕內判定色 0、hover-only `title` 提示 0、console error 0、`news.google.com` 0 | `docs/test/ui_checklist.csv`（未執行列 UI-1-086～UI-1-089）、`docs/test/screens/`（85 張）、`raw/ui_dom_audit_summary.txt` |
| UI-2 | P0 | 本次執行 | 通過 | 360×780 開 `/`、`/r/{id}`、`/trending`、`/knowledge`：`scrollWidth` 皆 360（無橫向捲動）；結果頁動作列 `position: sticky`，頂端時 595–724 px 可見、捲到底時 443–572 px 仍在分頁列（top 723）之上且未遮住頁尾 disclaimer（403–419）；分頁列 `position: fixed` 貼齊視窗底並套 `env(safe-area-inset-bottom)`（模擬器 inset 為 0，瀏海機實機留待 UI-7） | `raw/ui2_ui4_checks.txt`；`screens/S2_ui2-top_360_light.png`、`S2_ui2-bottom_360_light.png` |
| UI-3 | P1 | 本次執行 | 通過 | ① `src/**/*.jsx` 中引用判定色 token 的元件檔 **2 個**（≤3）：`components/Banner.jsx`、`components/InputCard.jsx`；燈號色集中於 `lib/verdict.js` 的 `toneOf()`。② 81 張截圖 DOM 檢查：導覽與動作按鈕內判定色 **0**；判定色只出現在燈號區、燈號點／判定字、未證實橫幅與 UNVERIFIABLE 黃框。觀察（非判定依據）：`backend_down` 橫幅（mockup brief §11 指定）與 inline 驗證紅字（spec §8.3 S1 指定）使用判定色 token → 登錄 DEF-08，待 6.4 界定 | `raw/ui_dom_audit_summary.txt`；`screens/S1_backend_down_*`、`S1_validation_*` |
| UI-4 | P0 | 本次執行 | 通過 | 全新設定檔、無 `fcc_theme`：系統淺色 → `data-theme=light`（body `rgb(255,255,255)`）；系統深色 → 自動 `dark`（`rgb(15,17,21)`）。按「切換深淺色」後主題翻轉並寫入 `fcc_theme`，`Page.reload` 後保持（light→dark→重整仍 dark；dark→light→重整仍 light） | `raw/ui2_ui4_checks.txt`；`screens/S1_ui4-system-light-toggled-reloaded_375_dark.png`、`S1_ui4-system-dark-toggled-reloaded_375_light.png` |
| UI-5 | P1 | 報告後執行 | 報告後執行 | 無障礙基準票 P-18 排報告後；Lighthouse 與 axe 本機未安裝且本輪不新增安裝。鍵盤預檢達標（Tab 依序走完首頁各焦點、每站 `outline: solid 2px`；於送出鈕按 Enter 後 2.4 s 進入 `/r/{id}` 並顯示「快取命中・相同內容」） | `raw/ui5_keyboard_walkthrough.txt` |
| UI-6 | P0 | 本次執行 | 未執行（附原因） | **未執行原因**：① 簡體 OpenCC `t2s` diff 因本機未安裝 OpenCC、本輪不新增安裝而未做；② `grep -P '[a-zA-Z]{4,}'` 準則須先界定允許清單（命中字多為 §8.7 文案原文），依 3.3 註 2 須經 6.4 確認；③ 逐 key 在介面出現未系統化比對，本輪截圖只涵蓋 S1–S4。靜態逐字比對達標：自 spec §8.7 解析 **139 個 key**，**135 個**與 `STRINGS` 逐字相同，其餘 4 個為表格寫法差異（`confidence_chip` 實作拆為 high／mid／low、`frame_yellow_no_source`、`knowledge_stats`、`nav_bot` 之儲存格含括號註解，文案本體一致），**缺漏 0**。英文字掃描命中：品牌 Threads、MyGoPen、Cofacts、LINE、Meta，以及 Cookie、token、THREADS_MODE、WEBP、App Review 與 `{summary}`、`{total}` 等占位符 | `raw/ui6_i18n_static_check.txt` |
| UI-7 | P1 | 報告後執行 | 報告後執行 | 需真手機與 Threads App 內建瀏覽器。本機已確認分享鈕 href 格式為 `https://www.threads.com/intent/post?text=…&url=…`（攔下開新分頁，未對 threads.com 發出請求） | `raw/ui_dom_audit_summary.txt`；`screens/S2_shared*` |
| UI-8 | P0 | 本次執行 | 通過 | ① 10 筆真實結果頁：每頁「查核來源」皆帶「查核機構」小標，網域 mygopen.com×6、tfc-taiwan.org.tw×2、165.npa.gov.tw×1、cofacts.tw×1，**無 Tier 3 網域**；以 `tier_of(offline=True)` 複核得 Tier 1×9（cofacts 文章依 offline 設計回 3，其在清洗報告為「有回覆」，存入之 Tier 1 有效）。② S3 截圖 12 張 DOM 皆無 `news.google.com`。③「尚無查核機構證實」狀態：SAFE 黃＋橫幅 4 張（375／1280 × 淺／深）、紅＋橫幅 2 張，已入檢核表 | `raw/ui8_source_tiers.txt`；`screens/S2_ui8-01…10_375_light.png`、`S2_unverified_safe_*`、`S3_*` |
| PF-1 | P0 | 本次執行 | 未執行（附原因） | **未執行原因**：未命中 p50／p90 之準則方法為 `evaluate.py --timing` 150 筆，該旗標（票 B-23）未合入 HEAD，本輪未依準則方法量測。兩個子準則達標：**L1 hash**（32 筆已入庫原文，準則要求 10 筆；另 1 筆暖機不計）32／32 `cache_layer=hash`、0 次 AI，用戶端 p50 **213 ms**／p95 257 ms、後端 p50 156 ms（準則 <1.5 s）；**L2 向量**（13 筆命中）用戶端 p50 **3.06 s**／p95 4.72 s、後端 p50 3.00 s（準則 <4 s），亦達 spec §9「快取命中 p95 ≤6 s」。未命中僅 5 則補充樣本（用戶端 p50 8.84 s、p90 12.54 s、最大 14.73 s；當輪 14 次 AI 呼叫後端 p50 9.94 s、p90 12.46 s），**樣本數與方法皆不符準則**，不作判定依據 | `raw/pf_requests_2026-09-16.csv`、`raw/pf_backend_structured_log_8030.txt` |
| PF-2 | P0 | 本次執行 | **未通過** | 20 筆改寫句送 `/api/analyze/sync`：`cache_layer=vector` **13／20 = 65%**，低於準則 ≥70%（差 1 筆）。命中 13 筆之 `kb_id` 皆為對應原文、燈號皆為預期紅燈；離線以同一 embedding 對 102 筆 1536 維向量複算，命中預測與線上結果逐筆一致。**未命中 7 筆之最高相似度**：0.7314、0.7213、0.7175（**3 筆貼近門檻 0.75**）與 0.6894、0.6841、0.5989、0.5748（**4 筆為脫離上下文的短句詐騙話術片段**，語意本就分散）。**資料說明**：本組改寫句由 Claude Code 為本次量測撰寫，非票 O-11 所指之組員審定版本、未經逐筆確認；量測後未依相似度回頭修改句子（避免結果偏差）。**改善計畫**：① 依票 O-11 由張宇宏產出、石岱勳逐筆確認之改寫句題組後重測；② 重算 DEF-05 之 11 筆非 1536 維向量，使可參與語意命中之列由 102 筆回到 113 筆；③ 評估 `bge-m3` 等中文向量模型，或以獨立保留組（held-out set）重新校準門檻。**不以「為了通過而調低門檻」處理**——0.75 係實測校準值，調低會提高誤命中風險 | `docs/test/pf2_paraphrases.csv`、`raw/pf_requests_2026-09-16.csv`（section=vector，含離線相似度欄） |
| PF-3a | P0 | 本次執行 | 通過（待補驗） | 模擬模式 3 輪：文字 mention（m1）`handle_mention` 由取出到回覆寫入 `elapsed_ms` = **205／215／264 ms**（準則 ≤60 s）；固定文案 m2 26／22／25 ms、m3 27／27／24 ms；第 3 輪自 `--poll` 啟動到 3 則回覆全數寫入 1.33 s。待補驗：live 3 次（3.4 註 1，報告後） | `raw/pf3a_threads_sim_rounds.txt`、`raw/pf_backend_structured_log_8030.txt` |
| PF-3b | P1 | 報告後執行 | 報告後執行 | 需 Threads 實機 @ 回覆（含 Meta 索引延遲，不可控）；未達標只記錄不擋 | — |
| PF-4 | P1 | 報告後執行 | 報告後執行 | 本週僅 3 則模擬 mention fixture（需 10 則），且 `THREADS_MAX_REPLIES_PER_POLL` 預設 5 需先調整 | — |
| PF-5 | P1 | 本次執行 | 通過 | ① 150 筆評測總花費：以閘道計價（輸入 USD 1.5／百萬、輸出 USD 9／百萬 tokens）估算評測自身上限 **USD 0.943**（≤1.5），約 USD 0.0063／筆。② 每次未命中平均：本輪 15 次 AI 呼叫合計約 USD 0.0954、平均 **USD 0.0064／次**（≤0.02），與評測估算一致。交付缺口：`docs/test/cgu_usage.txt` 未建立，用量改以 `/me/usage` 快照與後端結構化 log 之 `usd` 欄記錄（另見 DEF-06：後端價目表低估約 50%，本節數字均已改以閘道計價換算） | `raw/qa1_cgu_usage.jsonl`、`raw/opuipf_cgu_usage_snapshots.jsonl`、`functional_quality` §4.5 |
| PF-6 | P1（可選） | 報告後執行 | 報告後執行 | 可選比較組（`CGU_MODEL=gpt-5.4`），預算保留至報告後；不作通過條件 | — |
| QA-1a | P0（閘門） | 本次執行 | 通過 | CGU `gpt-5.4-mini`（鏈 `['cgu']`）150 筆、有效 **150**／分析失敗 **0**：**FN = 0**（TP 100／FP 0／TN 50）；執行 03:14:22–03:29:32、約 15 分 10 秒；跑前後各查一次 `/me/usage`。**執行偏差**：依當次指示未加 `--seed-db`（避免與並行測試同時寫入知識庫，單寫者假設），該旗標只影響判對案例是否回寫知識庫，不影響預測與指標 | `raw/qa1_evaluate_run.txt`、`code/backend/data/eval_binary.csv`、`eval_predictions.csv` |
| QA-1b | P1（量測） | 本次執行 | 通過 | accuracy **1.000**（目標 ≥0.93；95% CI 0.976–1.000）、FP **0**（目標 ≤8）、macro-F1 **1.000**、三類 P／R／F1 皆 1.000、信心中位數 0.97。與前一版並列：舊 `gpt-5-mini`（myai168，2026-06）accuracy 0.960、FN 0、FP 5；兩模型判定不同之 6 筆全為「舊錯新對」、0 筆「舊對新錯」，精確 McNemar 雙尾 **p = 0.031**。**限制**：150 筆題庫對新模型已飽和（滿分、CI 下限 0.976），無法再區分模型或設定，且題庫自 2026-06 起公開於 repo，無法排除被新模型見過 → 改善項：擴充題庫（例如 300 筆）並做信心校準後重測（CLAUDE.md §8 選項） | `raw/qa1_evaluate_run.txt`、`raw/qa1_old_vs_new_comparison.txt`、`code/backend/data/eval_report.csv`、`assets/confusion_matrix.png` |
| QA-2 | P1 | 本次執行 | **未通過** | ① `--report-only` 重算一致：`eval_report.csv`、`eval_binary.csv`、`confusion_matrix.png` 重算前後 sha256 完全相同（達標）。② 報告仍缺 `provider／model／date／use_web_search` 欄、預測檔無 `elapsed_ms`、`evaluate.py` 無 `--timing`（票 B-23 未合入）→ **未達標**。③ 量測時 `eval_errors.csv` 因「0 筆錯誤即 return、不重寫」而殘留舊模型 6 筆、與新報告矛盾；**量測後已修正**：0 筆錯誤時輸出只有表頭的空檔（commit `b1fc5c3`）。②之欄位與 `--timing` 仍待 B-23，故本項仍判未通過 | `raw/qa2_report_only.txt`、`raw/qa1_post_run_checks.txt`；`code/backend/scripts/evaluate.py` |
| QA-3 | P0 | 本次執行 | 通過 | 量測時最新 run **35008720030**（HEAD `c114413`）之 `test`（pytest，19 s）與 `frontend`（npm ci、build、unit tests）兩個 job 皆 success；其後 `b1fc5c3`（35015937421）與宣告版本 `ad13f2f`（**35019722949**）亦皆 success；2026-09-14 重做起算 17 次 run 全數 success；`ci.yml` 未寫死測試數 | `raw/qa3_gh_run_list.txt`；`.github/workflows/ci.yml` |
| QA-4 | P1 | 報告後執行 | 報告後執行 | 文件同步票 B-25 排 09-24～09-25。預查結果：`.env.example` 對應項全數一致；`CLAUDE.md` 與 `README.md` 於勾核表 8 個指定項目**全部不符**（多 provider 描述、STT 設定鍵、`ENABLE_THREADS_BOT`、Threads 權限清單、`legacy/` 路徑、測試數「31」對實際 478、timeout「150 s」對 `config.py` 之 60、種子提交規則）；交付物 `docs/test/doc_review.md` 未建立 | `raw/qa4_doc_review_grep.txt`、`functional_quality` §3.2 |
| QA-5 | P0 | 本次執行 | 通過 | 數字與資料實測一致：知識庫種子（commit `db8c62d`）218 筆、`verified` 128（MISINFO 75／SCAM 29／SAFE 24）、未證實 90；熱門牆 24 筆全數 verified、`news.google.com` **0**；清洗前後 236 → 218、63 → 24；第二次 apply 各規則影響 0、`label_source="rule"` 被降級 **0 筆**。**證據缺口（不改判定，已登錄）**：① dry-run 之逐筆 before→after 報告已被第二次 apply 覆寫，現存報告無法核對準則所寫之「Cofacts 33 筆」（現存為 23 篇：有回覆 19／無回覆 4）；② 無「隨機抽 10 筆降級列逐筆判斷」之書面紀錄，審閱文件記錄的是三項決定之整體核准與本地模型 300 筆第二意見；③ 審閱文件「113 筆帶向量」實際可參與語意命中者為 **102 筆**（DEF-05） | `raw/qa5_data_verification.txt`、`docs/test/clean_sources_review.md`、`code/backend/data/clean_sources_report.txt`、`check_after_d05.txt` |

**4.4 交付項目落差（供文件審查檢查完整性）**：本輪之證據以原始紀錄檔形式交付於 `docs/test/results/`（兩份執行結果彙整檔與 `raw/` 40 個原始輸出）、`docs/test/screens/`（85 張截圖）與 `docs/test/ui_checklist.csv`（89 列）。4.4 所列之下列彙整檔**尚未建立**，其內容目前以括號內的原始檔替代：`op_log.md`（`operation_ui_performance_2026-09-16.md` §2 與 `raw/op*.txt`）、`eval_summary.md`（`functional_quality_2026-09-16.md` §4 與 `raw/qa1_*`）、`perf_log.csv`／`perf_summary.md`／`threads_latency.csv`（`raw/pf_requests_2026-09-16.csv`、`raw/pf_backend_structured_log_8030.txt`、`raw/pf3a_threads_sim_rounds.txt`）、`cgu_usage.txt`（`raw/qa1_cgu_usage.jsonl`、`raw/opuipf_cgu_usage_snapshots.jsonl`）、`ui6_copy_check.txt`（`raw/ui6_i18n_static_check.txt`）、`ci_run.png`（`raw/qa3_gh_run_list.txt`，CI 截圖未擷取）；`deploy_log.md` 待 OP-8 執行後產出，`doc_review.md` 待 QA-4（報告後）產出。是否要求補齊彙整檔由 6.4 審查決定。

### 6.3 缺陷與處置

嚴重度定義：高＝使 P0 準則無法達成或中斷 demo 主流程；中＝影響 P1 準則或有替代操作；低＝外觀、文案、舊資料等不影響本輪判定者。

| 編號 | 缺陷描述（發現來源） | 發現日期 | 嚴重度 | 原因與處置（含回歸驗證） | 狀態／預計完成 |
|------|----------------------|----------|--------|--------------------------|----------------|
| DEF-01 | 送出查證後，結果頁偶發顯示「出了點問題 伺服器發生錯誤（500）」並停止輪詢（demo 影片錄製 take S7_t1，`docs/demo/footage_log.md` §4 發現 1） | 2026-09-16 | 高（FR-04 結果頁；demo 主流程） | 原因：任務處理器改寫 `tasks.parquet` 的同時 `GET /api/result/{id}` 讀到寫入中（0 byte）之檔案，pyarrow 解析失敗。處置：新增 `app/utils/parquet_io.py`（先寫暫存檔再 `os.replace` 原子替換、讀取失敗短暫重試、同檔「讀→改→寫」行程內加鎖），`TaskStore` 與 `PandasStore` 全面改用（commit `c114413`）；Windows 上並發測試 4 次仍偶發 1 次 `PermissionError`，再將重試視窗加長至約 5 秒（commit `b1fc5c3`）。回歸：`tests/test_parquet_io.py`（暫存檔不殘留、讀取重試、8 執行緒並發更新＋讀取不遺失且不報錯）；該檔單獨連跑 **10／10 通過**；宣告版本全套 **478 passed**；CI run 35019722949 綠；本輪 8030 存取 log 328 筆中 **5xx = 0** | **已修正、回歸通過**（2026-09-16） |
| DEF-02 | v1.0 前的舊任務若 AI 失敗，`GET /api/result/{id}` 仍回 `ai_unavailable=false`，結果頁顯示黃框判定與「分享到 Threads」；舊 `tasks.parquet` 28 筆 completed 中 17 筆為 fallback（OP-7 發現；執行結果檔記為 D-1） | 2026-09-16 | 低（僅影響 v1.0 前之舊資料，不在 demo 範圍；但會把「AI 分析暫時無法使用」當成有效判定對外分享） | 原因：舊任務無 `ai_unavailable` 欄、載入時補 False，API 未由結果內容判斷。處置：`app/api/result.py` 改以 `verdict.is_fallback()` 推導 `ai_unavailable`（前綴只留給舊碼相容，符合 spec §5.5），`share` 隨之為 null（commit `ad13f2f`）。回歸：新增 `tests/test_result_api.py::test_legacy_fallback_task_marked_ai_unavailable`；後端 **478 passed**；CI run 35019722949 綠 | **已修正、回歸通過**（2026-09-16） |
| DEF-03 | 知識庫頁同一主張出現兩張卡且標籤不同：`484ad500`（TEXT，假訊息）與 `cac3a014`（URL，詐騙）、`2ac98c6c`（TEXT，假訊息）與 `95acaa79`（URL，詐騙）；四列皆 `verified=true`，為提交種子既有資料（UI-1 之 S4 截圖觀察） | 2026-09-16 | 低（資料；不影響判定，但同一主張兩種標籤有損可信度） | 原因：同一 Cofacts 文章分別以 TEXT 與 URL 兩種輸入入庫，各自判定。處置：清洗規則加「同一 Cofacts 文章之 TEXT／URL 列去重或統一 `risk_type`」，或以 `/api/admin` 覆寫；與 CLAUDE.md §9「同一謠言兩種標籤」同類。未規劃回歸測試前先以 `test_clean_sources.py` 新增案例覆蓋 | 未處理；**報告後**（隨清洗規則調整，目標 09-24～09-30） |
| DEF-04 | 部分已證實列之「主張」其實是查核機構文章標題卻被標為假訊息：`5b12ca94`（「解析『馬鈴薯之亂』…」）、`c1ca6a6a`（「臉書詐騙廣告年賺5000億」）；另 `c792a37e`（「禧年探索資本…已被金管會通報為高風險投資詐騙黑平台」）讀來可能是正確警示（UI-8 抽查頁 07、08、09） | 2026-09-16 | 低（資料，待人工確認；UI-8 判定不受影響） | 原因：查核報導標題入庫時之判定對象為報導本身而非被查核主張（CLAUDE.md §9 三道防線之殘留案例）。處置：由石岱勳逐筆確認；誤標者改 `risk_type` 或以 `/api/admin` 移出知識庫；`c792a37e` 若確為正確警示則保留並註記 | 未處理；**報告後**（待人工判斷） |
| DEF-05 | 128 筆已證實列中 **11 筆**向量維度為 768（5 筆）／3072（6 筆），`find_similar_by_vector` 只比對同維度，實際可參與語意命中者為 **102 筆**；`clean_sources_review.md` 與 2.2 受測範圍表記為 113 筆（QA-5 核對、PF-2 離線複算） | 2026-09-16 | 中（壓低 PF-2 向量命中率，且使 QA-5 引用之文件數字與資料不符） | 原因：不同時期以不同 embedding 模型寫入之殘留列，清洗未檢查維度。處置：以 `text-embedding-3-small` 重算這 11 筆向量（11 次 embedding，估 <USD 0.001）並更正審閱文件與 2.2 之數字；完成後再執行 PF-2 重測 | 未處理；**報告後**（PF-2 重測前完成，10-05 前） |
| DEF-06 | 後端 `MODEL_PRICING_PER_1M["gpt-5.4-mini"]`（USD 0.75／4.50 每百萬 tokens）為 CGU 閘道實際計價（USD 1.5／9）的一半，結構化 log 的 `usd` 欄低估約 50%（PF-5 成本核對） | 2026-09-16 | 低（僅影響成本估算；本節與 PF-5 之數字已改以閘道計價換算） | 原因：價目表未隨閘道計價更新。處置：更新價目表並回算歷史 log 之估算值；與 spec §9 成本段一併校對（含 `CGU_REASONING_EFFORT` 鍵名） | 未處理；**報告後**（隨 QA-4 文件同步，09-24～09-25） |
| DEF-07 | 每次寫入知識庫時 `app/services/pandas_store.py:281` 之 `pd.concat` 印出「空欄或全 NA 欄之 dtype」`FutureWarning`（全套 pytest 440 warnings 之大宗） | 2026-09-16 | 低（外觀；升級 pandas 後可能改變欄位型別） | 原因：`pd.concat` 併入全 NA 欄，pandas 2.x 標記為棄用行為。處置：寫入前明確指定欄位 dtype 或先濾掉全 NA 欄，並於升級 pandas 前確認型別不變 | 未處理；**報告後** |
| DEF-08 | `backend_down` 橫幅（mockup brief §11 指定 yellow／yellow-soft）與首頁 inline 驗證錯誤紅字（spec §8.3 S1 指定紅字）使用判定色 token，與 spec §8.1「紅黃綠只用於判定」之字面衝突（UI-3 觀察） | 2026-09-16 | 低（規格文字衝突，非實作缺陷；UI-3 依準則仍判通過） | 原因：spec §8.1 之顏色紀律與 §8.3、mockup brief §11 之指定互相牴觸。處置：**需由 6.4 審查擇一**——(a) 在 §8.1 加註例外（橫幅與表單驗證可用語意色）並維持現行實作，或 (b) 改用中性／非判定色 token 並重擷相關截圖 | **待審查決定**（6.4） |

殘餘風險：

1. DEF-01 之鎖僅在單一行程內有效，跨行程同時寫入仍依單寫者假設以排程避免（4.3 第 4 列）。
2. FN-4 之三項未實作行為屬實機模式：在取得 Threads 實機 token 前不影響 demo，但 live 上線後可能造成 429 後持續重試、未知 4xx 被當成可讀取失敗、以及 container 未 `FINISHED` 即 publish 而漏發；此三者為報告後 live 測試（OP-5、PF-3b）之前置條件。
3. PF-2 未通過與 DEF-05 相關但不等同：即使 11 筆向量修復，仍有 4 筆短句話術片段之相似度 ≤0.69，需靠題組審定與模型／門檻評估處理。

### 6.4 審查結論與簽核

**結論（彙整，待審查者裁定）**：本輪於 2026-09-16 對 46 個測試項目中之 34 項（第 3 節指定本次執行者）實際執行，完成 24 項通過、2 項通過（待補驗）、3 項未通過、4 項未執行，其餘 13 項依範圍列為報告後執行。已驗證之主要結果為：CGU `gpt-5.4-mini` 150 筆評測 **accuracy 1.000、macro-F1 1.000、FN 0、FP 0**（有效預測 150／150，對照前一版 `gpt-5-mini` 0.960／FN 0／FP 5，精確 McNemar p = 0.031），**絕對閘門 QA-1a 達成**；後端 `pytest tests` **478 passed**、CI（`test`＋`frontend`）於宣告版本 `ad13f2f` 綠燈（run 35019722949）；三層快取 L1 命中用戶端 p50 **213 ms**、L2 向量命中 p50 **3.06 s**；Threads 模擬模式閉環回覆 **205–264 ms**；來源紀律與清洗後資料（知識庫 218 筆／已證實 128 筆、熱門牆 24 筆、Google News 轉址 0 筆）逐項核對相符；84 列介面檢核格全部符合且無 §8.6 違規。**未達 4.2 通過準則**：P0 通過 23／30——**FN-4**（Threads 429 backoff、未知 4xx 分流、container 兩段式發佈三項行為未實作，票 T-12／T-13，目標 09-24～09-30）與 **PF-2**（向量命中率 65% < 70%，改善計畫見 6.2）未通過，**OP-1、UI-1、UI-6、PF-1** 未執行（需全新 clone、非實作組員勾核、OpenCC 與允許清單界定、`--timing` 前置票），**OP-8** 由本次執行改列報告後；本次執行之 P1 通過率 66.7%（2／3，不含記錄性項目）亦未達 ≥80%。缺陷方面 DEF-01、DEF-02 已修正並通過回歸，DEF-03～DEF-07 排報告後處理，DEF-08 為規格文字衝突需本次審查裁定。建議之處置為「有條件通過」：以 QA-1a 閘門達成與回歸全綠為基礎，允許進度報告與 demo 影片引用上列已驗證數字，但報告中須明列 FN-4、PF-2 未達標與四項未執行項目及其時程；程式凍結與 tag `v0.3.0-demo` 於 09-20 前視 OP-1、OP-8、UI-1、UI-6、PF-1 補齊情形決定。最終結論由下表審查者裁定。

| 審查項目 | 結論 |
|----------|------|
| 全部 P0 項目是否通過 | **否**：P0 通過 23／30（含通過（待補驗）2）；未通過 2（FN-4、PF-2）、未執行 4（OP-1、UI-1、UI-6、PF-1）、報告後執行 1（OP-8，原列本次執行） |
| 本次執行之 P1 項目通過率是否 ≥80%（依 4.2 計算） | **否**：66.7%（分母 3＝UI-3、PF-5、QA-2，已扣除記錄性項目 QA-1b；通過 UI-3、PF-5，未通過 QA-2） |
| QA-1a FN=0 閘門是否達成 | **是**：FN = 0、有效預測 150／150（`errored` 0）、FP 0、accuracy 1.000；偏差：未加 `--seed-db`（不影響預測與指標），請審查者確認接受 |
| 準則修訂（4.2）與 UI-6 範圍界定（3.3 註 2）是否同意 | 待審查裁定。待確認事項：① 4.2 已登錄之 OP-1、OP-9、FN-1、QA-1b 四項修訂；② UI-6 之 `[a-zA-Z]{4,}` 允許清單（品牌 Threads／MyGoPen／Cofacts／LINE／Meta 與 Cookie、token、THREADS_MODE、WEBP、App Review、`{占位符}`）；③ spec §10.2 FN-1 原文回寫 B-03、B-22 兩項例外；④ DEF-08 之 §8.1 顏色紀律例外 |
| 部分延後準則與報告後執行項目之目標日期（4.5）是否同意 | 待審查裁定。含本輪範圍變更：OP-8 由 09-18 移至報告後；OP-3、PF-3a 之待補驗項（S-11、live 量測）；FN-4 依 T-12／T-13 排 09-24～09-30；PF-2 重測排 10-05 前 |
| 未通過項目與缺陷（6.3）之處置是否完成 | **部分**：DEF-01、DEF-02 已修正且回歸通過；DEF-03～DEF-07 排報告後；DEF-08 待本次裁定；FN-4、PF-2 之修正與重測均排報告後，本輪未完成 |
| 整體結論（通過／有條件通過／不通過） | 待審查裁定（彙整建議：**有條件通過**，條件為上列結論段所述之報告揭露與補齊時程） |
| 審查意見 | 待審查者填寫 |

| 角色 | 姓名 | 日期 | 簽名 |
|------|------|------|------|
| 測試負責人 | 石岱勳 | | |
| 文件審查 | 廖昱翔 | | |
| 審查者（指導教授） | | | |
