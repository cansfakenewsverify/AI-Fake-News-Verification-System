# 1 名稱與編號

| 項目 | 內容 |
|------|------|
| 文件名稱 | 全民查證公社 AI 假訊息與詐騙查證系統 測試計畫書 |
| 文件編號 | `TP-FNV-2026-01` |
| 版本 | v0.1（第 1–3 節初稿；第 4–5 節由 O-15 撰寫，第 6 節由 O-16 填寫並升 v1.0） |
| 日期 | 2026-09-15 |
| 撰寫依據 | `docs/rebuild/00_consensus.md` §7（計畫書納入流程）、`docs/rebuild/01_spec.md` v1.3 §1、§4、§10 |
| 受測系統 | 全民查證公社（網站前端 React + FastAPI 後端 + Threads 機器人 `@factcheck_tw_bot`） |

本計畫書各節與規格書的對應（共識 §7、spec §0「下游文件」）：

| 計畫書章節 | 來源 |
|------------|------|
| 2.3 參考文件 | spec §10.6「2.3 參考文件」 |
| 3.1 操作測試 | spec §10.1 |
| 3.2 功能測試 | spec §10.2（`pytest tests`，CI 自動）；FR 編號引用 spec §4 |
| 3.3 介面測試 | spec §10.3（visual review） |
| 3.4 績效測試 | spec §10.4（`evaluate.py --timing` + 後端 log + 手動計時） |
| 3.5 品質測試 | spec §10.5（`evaluate.py`） |
| 4.1 測試環境、4.4 交付項目 | spec §10.6 |
| 4.2 通過準則、4.3 中止與再繼續條件 | spec §10「4.2 通過準則彙總」與 §10 開頭 |

# 2 簡介

## 2.1 目的與範圍

**目的**：驗證「全民查證公社」重做版本（一週重做，2026-09-15 起）是否達到進度報告與 3 分鐘 demo 影片的交付條件，並以可量測、可重跑的準則留下證據，供報告與論文引用。

**產品定位**（spec §1）：台灣繁中的假訊息／詐騙查證平台——貼上文字或網址（圖片 P1），AI 判定 SCAM／MISINFO／SAFE 並附查核來源；同一套判定在 Threads 上 @機器人 即可取得，網站結果可一鍵分享回 Threads。

**成功定義**（spec §1，本計畫的測試目標）：

1. 進度報告 + 3 分鐘 demo 影片如期交出。
2. 影片完整走一遍閉環：Threads 可疑貼文 → Tester 回覆並 @機器人 → 機器人回覆紅／黃／綠燈號 + 來源 + `/r/{id}` 連結 → 點開手機版結果頁 → 「分享到 Threads」回到 Threads。Meta 憑證未到時，模擬 Threads 模式（FR-10）呈現同一閉環。
3. 網站四個主畫面（首頁／結果頁／熱門牆／知識庫）在手機寬度可用；淺色預設；紅黃綠只出現在判定。
4. AI 真的能跑（CGU AIR `gpt-5.4-mini`，provider 鏈 `['cgu']`）：至少展示一次「未命中快取 → AI 判定」與一次「改寫版命中向量快取（`cache_layer="vector"`）」；全程零「AI 分析暫時無法使用」畫面。
5. `pytest tests` 全綠、CI 通過；評測數字（舊 accuracy 96%／FN=0 與 CGU `gpt-5.4-mini` 新跑值並列）與三層快取證據可在報告引用。
6. demo 資料乾淨：出現的每一個「查核來源」都是 Tier 1／2；熱門牆無 Google News 轉址；知識庫頁不出現 `verified=false` 的記錄；找不到查核來源時畫面明示「尚無查核機構證實」且不出現綠燈。

**測試範圍**：spec §4 的 FR-01～FR-19 中列為 P0 者為必測；P1 條目以「P1 ≥80%」計入通過（見 4.2）；P2 條目（FR-12 每日查核貼文、`/history` 全頁、速率限制）不在本次測試範圍。測試分五類：操作（3.1）、功能（3.2）、介面（3.3）、績效（3.4）、品質（3.5）。

**非目標（不測）**（spec §1、共識 §2「不做」）：影片查證（yt-dlp + Whisper）、FB/IG 封閉平台（Playwright）、文字輸入的 Google 搜尋抓取、熱門牆的 Google News 來源、管理者覆寫 UI（`/api/admin` API 保留）、使用者帳號／登入、英文介面、Threads Webhook、Threads 私訊、公開讓陌生人 @機器人（需 App Review + 企業驗證，論文寫明為上線條件）。

## 2.2 系統概述

| 元件 | 說明 | 對應 FR |
|------|------|---------|
| 網站前端 | React + Vite + Tailwind（`code/frontend`），部署於 Vercel；畫面：首頁 `/`、結果頁 `/r/{id}`、熱門牆 `/trending`、知識庫 `/knowledge`、機器人狀態 `/bot`、靜態頁 `/privacy.html`／`/data-deletion.html`／`/deauthorize.html`。前端只讀後端 `frame_type`／`frame_label` 顯示燈號，查證歷史存 localStorage（首頁最近 5 筆）。 | FR-04～08、FR-11、FR-13 |
| 後端 API | FastAPI + Uvicorn（`code/backend`，單 worker），執行於負責人電腦，經 Cloudflare quick tunnel 對外；Vercel `vercel.json` rewrites 將 `/api` 同源代理到 tunnel。查證送出後立即取得 `result_id`，結果頁輪詢 `GET /api/result/{id}`。 | FR-01、FR-02、FR-04 |
| 三層快取 | 文字輸入：L1 內容 SHA-256 hash → L2 向量（`text-embedding-3-small` 1536 維，以使用者原文比對，門檻 0.75，只比對 `verified=true` 列）→ L3 AI。網址輸入：L0 URL → L1 hash → trafilatura 爬取（SSRF 防護）→ L2 向量（以爬到內文）→ L3 AI。結果回填 `knowledge_base.parquet`；回應帶 `cached`／`cache_layer`（url／hash／vector）。文字輸入不再送 Google 搜尋。 | FR-01、FR-02、FR-17 |
| AI 引擎 | CGU AIR Gateway（OpenAI-compatible Responses API，`AI_PROVIDER=cgu`、`CGU_MODEL=gpt-5.4-mini`）；用量以 `GET {CGU_BASE_URL}/me/usage` 查詢；不使用 OpenAI 直連、myai168 已停用。provider 全失敗時回 HTTP 200 fallback（`ai_unavailable=true`、summary 以「AI 分析暫時無法使用」開頭），不寫知識庫。系統 prompt 含來源規則（FR-19）。 | FR-01、FR-14、FR-19 |
| 來源分級 | `app/utils/source_tier.py`：Tier 1 已有判定的查核機構（TFC、MyGoPen、有 RUMOR／NOT_RUMOR 回覆的 Cofacts 文章、`*.gov.tw` 等官方機構）；Tier 2 主流媒體查核報導（呼叫 `_title_indicates_debunk()`）；Tier 3 其餘。對外只顯示 Tier 1／2；無 Tier 1／2 來源時 `verified=false`、顯示「尚無查核機構證實」且不得綠燈。知識庫寫入門檻與既有資料清洗腳本（`clean_sources_2026_09.py`）確保 demo 資料乾淨。 | FR-16、FR-17、FR-18 |
| 紅黃綠映射 | `frame_of()` 單一權威（spec §7.8，8 列），Web 結果頁與 Threads 回覆共用。 | FR-04、FR-09 |
| Threads 機器人 | `@factcheck_tw_bot`，開發模式（未通過 App Review，只讀得到 Threads Tester 的提及與貼文；Tester 帳號須公開）。後端輪詢 `GET /{user_id}/mentions` → 讀原貼文 → 走與 FR-01 相同管線 → 依 spec §7.7 模板回覆燈號 + 來源 + `/r/{id}`（≤500 字）。`THREADS_MODE=live／sim／off`；`sim` 以 `FakeThreadsService` 讀本機 `mentions.json`、寫 `replies.jsonl`，作為 demo 保險與離線測試。 | FR-09、FR-10、FR-11 |
| 分享 | 結果頁「分享到 Threads」以 Web Intent `https://www.threads.com/intent/post?text=…&url=…` 開新視窗，純前端、零 token。 | FR-05 |
| 資料 | SQLite `factcheck.db`（熱門牆）、`knowledge_base.parquet`（三層快取知識庫，清洗後提交為種子）、`tasks.parquet`（結果頁，上限 5,000 筆）、`eval_set.csv`（150 筆評測集）。 | FR-06、FR-07、FR-18 |

## 2.3 參考文件

（逐條依 spec §10.6「2.3 參考文件」）

1. `docs/rebuild/00_consensus.md`（2026-09-14，含 2026-09-15 §4／§9 補充）
2. `docs/rebuild/01_spec.md`（v1.3）
3. `CLAUDE.md`
4. `code/backend/data/eval_report.csv`／`eval_binary.csv`／`eval_errors.csv`
5. `assets/confusion_matrix.png`
6. `data/clean_sources_report.txt`（FR-18）
7. Threads 官方文件（Threads API Overview、Reference > Publishing／Replies／Mentions、Get Started > Permissions／App Roles、Web Intents；以 threads_gap_analysis／threads_verify_10 記錄之 URL 為準）
8. CGU AIR Gateway 教學頁（`https://air.cgu.edu.tw/workspace4/LLMAPI/api_call.html`；OpenAI-compatible Responses API，`/v1/me/usage` 用量查詢）
9. OpenAI Responses API 文件（僅作格式參考；`web_search` `filters.allowed_domains` 於閘道的支援性見 OP-2）

# 3 測試項目

各表欄位：ID／優先級／對應 FR／通過準則（操作測試另保留「步驟」欄）。ID、優先級與準則文字與 spec §10 一致；「對應 FR」欄在 spec 功能測試表已列者照抄，其餘依準則內容對應 spec §4 的 FR 或章節。

## 3.1 操作測試

操作者：U5（負責人）。來源：spec §10.1。

| ID | 優先級 | 對應 FR | 步驟 | 通過準則 |
|----|--------|---------|------|----------|
| OP-1 | P0 | FR-15、FR-18、§5.6 | 全新 clone → `start.bat` | 後端 `/health` 200 且 `ai_available=true`；前端 5173 開啟首頁；<5 分鐘（含 pip/npm）；`.env` 已自動產生 ≥32 字 `ADMIN_TOKEN`，`curl -X POST -H "X-Admin-Token: …" /api/threads/poll` 回 202 或 200 `threads_disabled`；**`GET /api/knowledge/stats.total ≥ 150`**（證明提交的種子是 FR-18 清洗後版本，`verified` 已回填；在 OP-9 之後執行） |
| OP-2 | P0 | FR-19、FR-14 | `scripts/test_ai_provider.py --provider cgu`（關／`--web-search` 開／`--web-search --allowed-domains` 各跑一次；前後各查一次 `GET {CGU_BASE_URL}/me/usage`） | **P0 條件（只有這一項擋交付）**：關閉 web_search 的一次印出合法 `risk_type`、非 fallback、印出 `usage`，embedding 維度 1536。**記錄項（回 400／不支援不算不達標）**：`--web-search` 與 `--allowed-domains` 兩次結果寫入 `docs/test/cgu_usage.txt`——`--web-search` 回 400／閘道不支援 → `USE_WEB_SEARCH=false` 進 demo 並在報告註明；`--allowed-domains` 回 200 且 `url_citation` 全在清單內（另記 `gov.tw` 裸網域是否涵蓋子網域）→ 閘道支援，FR-19 維持 P1、D-16 決定是否 Day 6 順手做；否則不做。三次 `/me/usage` 前後差合計 ≤ USD 0.06 |
| OP-3 | P0 | FR-10、FR-11 | `THREADS_MODE=sim` + `scripts/test_threads_bot.py --poll`（或 `curl` 打 `/api/threads/poll` 後輪詢 `status`） | 一輪完成、`replies.jsonl` 新增 ≥1 行、`/bot` 頁 ≤4 秒顯示該回覆、`status.mode == "sim"` |
| OP-4 | P1 | FR-09（§7.3） | `scripts/threads_auth.py` 走完授權 | `data/threads_token.json` 產生，含 `authorized_at`，`token_days_left` 介於 55–60 |
| OP-5 | P1 | FR-09（§7.6、§7.7、§7.10） | `scripts/test_threads_bot.py --live` | profile 與 mentions 皆 200；log 一則真實 mention 原始 JSON（記下 `media_type` 實際值）；對機器人自己貼文試發 `auto_publish_text=true` 回覆一則並 `GET /{id}` 可讀（7.6 D-11 依據）；480 字純中文 + 3 emoji 試發一則（7.7 長度規則）；故意讀非 Tester 貼文、用壞 token 各一次並記錄狀態碼與 `error` body（7.10） |
| OP-6 | P1 | FR-09（§7.3） | `scripts/threads_auth.py --refresh` | `expires_at` 延後 ≥50 天（或拒絕：token <24 h） |
| OP-7 | P0 | FR-15、§6 | 以 v1.0 前的舊 `tasks.parquet`／`knowledge_base.parquet`／`factcheck.db` 啟動後端 | 載入成功、新欄位為預設值、筆數不變（`check_db.py` 前後比對） |
| OP-8 | P0 | FR-04、§8.2 | Vercel 部署 + quick tunnel + `vercel.json` rewrites | 手機 4G 開 `{PUBLIC_BASE_URL}/r/{任一 id}` 可看到結果；`{PUBLIC_BASE_URL}/api/health` 200（同源代理） |
| OP-9 | P0 | FR-18 | `scripts/clean_sources_2026_09.py --dry-run` → 負責人審閱 `data/clean_sources_report.txt`（D-17）→ `--apply` → 再 `--apply` 一次 | dry-run 不改任何檔（`git status`／檔案 mtime 不變）；報告列出各規則影響筆數與逐筆 before→after；apply 後 `check_db.py`：知識庫 236 筆不變、`verified=true` ≥150、熱門牆 `news.google.com` 0 筆、`.bak-{date}` 備份存在；第二次 apply 影響筆數 0（冪等）；`pytest tests` 仍綠 |

## 3.2 功能測試

執行方式：`cd code/backend && .\venv\Scripts\python -m pytest tests -q`（本機）與 GitHub Actions（CI 自動）。來源：spec §10.2。

| ID | 優先級 | 對應 FR | 通過準則 |
|----|--------|---------|----------|
| FN-1 | P0 | FR-01、FR-19 | 既有測試依 5.5／7.7／7.8 新契約更新後全過（**允許改寫** `tests/test_ai_service_contract.py` 的框色案例（改驗 7.8 表 **8 列**）與 Threads 回覆案例（改驗 7.7 新模板，含「尚無查核機構證實」替代行）；其餘 `test_cache_and_store`／`test_marking_rules`／`test_api`／`test_url_validator`／`test_processor_flow` 五檔**不得修改斷言**——`test_cache_and_store` 的 fixture 直接呼叫 `save_record`、無 `verified` 參數，其來源 `165.npa.gov.tw` 為 `gov.tw` Tier 1，由 `save_record` 內部離線計算得 `verified=true`（FR-17「計算位置」），`test_vector_search_exact_hit` 在 `find_similar_by_vector` 加 `verified` 過濾後仍原樣通過）+ 新增：文字輸入不呼叫 crawler（mock 斷言 `process_input` 未被呼叫）；系統 prompt 含 FR-19 逐字規則（字串比對「找不到這類來源就回 `sources: []`」） |
| FN-2a | P0 | FR-02 | `test_safe_url.py`：10 個私網／協定樣本全部拒絕（422 `blocked_url`）且未發出請求、3 個公網放行；連線逾時 10 s 與總逾時 `CRAWLER_TIMEOUT` 以 mock 各自驗證生效；爬取失敗回 UNVERIFIABLE 而非例外；非網址 422；輸入網址同網域的 `sources` 項被剔除。（`similar_news` 向量近鄰斷言隨功能降 P1 移至 FN-2c） |
| FN-2c | P1 | FR-02 `similar_news` | `similar_news` 由向量近鄰填入（固定向量 fixture）、只取 `verified=true` 列、排除自己、≥0.6 |
| FN-2b | P1 | FR-03 | 非圖片 415、>10 MB 413、bytes hash 命中 |
| FN-3 | P0 | FR-04、5.1 | `test_result_api.py`：`/api/result/{id}` 對 pending／completed／failed／404／ai_unavailable 五態 schema 正確；sync／async／threads_sim 三種來源皆 200；`/health` 含 `scheduler.{enabled,interval_hours}` |
| FN-4 | P0 | FR-09／FR-10 | `test_threads_bot_sim.py`：文字 mention 產生 `threads_len ≤ 400` 回覆且含 `/r/`；圖片 mention（`media_type=IMAGE`、無文字）→ `reply_media_only`；`get_post` HTTP 403 → `reply_cannot_read`；HTTP 429 → `backoff_until` 設定；未知 4xx → 標 failed 不回覆；二次 poll 不重複；fallback 不回覆不標記；自我貼文跳過（`username == bot` 與 `reply_id` 在本機清單兩種）；每則後 state 已寫入（crash 模擬）；container `FINISHED` 才 publish、`ERROR` 重試一次 |
| FN-5 | P0 | 7.7 | **七種**第一行模板（含 `🟡 尚無查核機構證實`）`threads_len ≤ 400`（實測後放寬 480）；4 個 emoji + 200 字 summary、含 ⚠️ 的 summary 兩個極端案例仍 ≤500 且保留結果頁連結；「🟡 尚無查核機構證實 + 替代行『尚無查核機構證實』（無連結）」一組長度案例：連結數 = 1（只剩結果頁）、替代行計入長度不計連結；連結數 ≤2；`threads_len` 對 BMP emoji／U+FE0F／U+200D 以 bytes 計；**實測長度規則紀錄欄**（Day 2.5 OP-5 結果） |
| FN-6 | P0 | 7.8 | `frame_of()` 對表格 **8 列** + 11 組 (is_risk, risk_type, confidence, ai_unavailable, verification_status) 輸入的表格測試；含「SAFE、0.95、unverified → yellow 尚無查核機構證實」與「SAFE、0.95、verified → green」、「缺 `verification_status` 鍵 → 視為 unverified」三組 |
| FN-7 | P0 | 5.5 | `is_fallback()` 統一判斷（含 `evaluate.py`／`test_ai_provider.py` 改呼叫）；`ai_unavailable` 與 summary 前綴同時成立 |
| FN-8 | P0 | FR-07 | `q="("` 200；`offset` 分頁不重複不遺漏；stats 四格加總 = total；fixture 中 `verified=false` 列不出現在列表、不計入 `total`（FR-17） |
| FN-9 | P0 | 5.6 | 管理端點無 token → 401；`ADMIN_TOKEN` 空 → 403；正確 → 202/200/409 |
| FN-10 | P1 | FR-14 | `DAILY_AI_CALL_CAP=1` 時第二次未命中回 429 且 AI mock 未被呼叫 |
| FN-11 | P0 | 三層快取 | 同文 hash 命中、改寫文 vector 命中（固定向量 fixture）；`verified=false` 列即使相似度 0.99 也不命中 vector 層（hash 層仍命中）；圖片 bytes hash 命中移至 FN-2b |
| FN-12 | P0 | FR-16 | `test_source_tier.py`：≥15 組 URL／標題 → tier 表格測試（`gov.tw.evil.com` → 3、`news.google.com/rss/…` → 3、Cofacts 有回覆 mock → 1／無回覆 → 3、MyGoPen 任意標題 → 1、ETtoday 查核標題 → 2、ETtoday 一般標題 → 3、Cofacts GraphQL 逾時 → 3、`offline=True` 時 Cofacts → 3 且未發請求）；GraphQL mock 對準 `https://api.cofacts.tw/graphql`（與 search_service.py:216 同端點）；`tier_of` 對 Tier 2 呼叫的是 `news_fetcher._title_indicates_debunk`（mock 斷言，防複製規則） |
| FN-13 | P0 | FR-17 | `test_kb_write_gate.py`：FR-17 驗收 1 的四組（Tier 1 存活 → true；只有 Tier 3 → false 且 vector 不命中；`sources=[]` + rule → true；Tier 1 但 validator 判死 → false）；`/api/result` 回 `verification_status` 三值正確；`sources[]` 無 tier 3 |
| FN-14 | P0 | FR-18 | `test_clean_sources.py`：fixture Parquet／SQLite 上 `--dry-run` 檔案 mtime 不變；`--apply` 兩次結果相同；Google News 解析 mock 成功（覆寫 + 去重）與失敗（刪列）兩種；`label_source="rule"` 列永不降級 |

整體目標（spec §10.2「目標」列，非獨立測試 ID）：P0 測試數 ≥60、CI 綠燈、執行 <60 s、零付費呼叫（Cofacts GraphQL 一律 mock）；FR-01／02／04～07／09／10／11／16／17／18 每條至少一個測試。

## 3.3 介面測試

執行方式：visual review（共識 §7），以截圖檢核表 `docs/test/ui_checklist.csv` 逐格勾核。來源：spec §10.3。

| ID | 優先級 | 對應 FR | 通過準則 |
|----|--------|---------|----------|
| UI-1 | P0 | §8.3、§8.4（FR-04、FR-06、FR-07、FR-11、FR-13） | 以 8.4 狀態總表為母版產出「截圖檢核表」（每格一列：畫面／狀態／寬度／主題／檔名／勾核者／結果）。P0 畫面 S1、S2、S3、S4、S6 的打勾格 × 2 寬（375／1280 px）× 2 主題 = 必交（約 100 張；S5 為 P2 不列）。「一致」定義：該格顯示 8.7 對應 key 的文案且無 8.6 列出的違規（字級 <13 px、hover-only 說明）；S1／S2 另與 Day 1 mockup 對照，S3／S4／S6 只對 8.3 元件清單。由非實作組員勾核 |
| UI-2 | P0 | FR-04、FR-05、§8.5 | 手機 360×780 無橫向捲動；結果頁動作列 sticky 可見；底部分頁列在安全區內 |
| UI-3 | P1 | §7.8、§8.1 | 顏色紀律：`grep -l 'c-verdict' src/**/*.jsx` 命中的元件檔 ≤3（ResultCard、Chip、燈號點）；導覽／按鈕不出現紅黃綠（截圖抽查） |
| UI-4 | P0 | §8.1 | 淺色為預設；系統深色時自動深色；切換後重整保持 |
| UI-5 | P1 | §8.6 | Lighthouse（手機）Accessibility ≥90、Performance ≥80；axe 零 critical；鍵盤 Tab 可走完首頁並完成一次查證 |
| UI-6 | P0 | §8.7 | 文案表 8.7 全部 key 在介面出現且逐字一致；`grep -P '[a-zA-Z]{4,}' src/i18n.js` 只允許品牌與 URL；簡體以 OpenCC `t2s` 轉換前後 diff 為空 |
| UI-7 | P1 | FR-04、FR-05 | Threads 手機 App 內建瀏覽器開 `/r/{id}`：燈號首屏可見；分享鈕開出的 URL 符合 FR-05 驗收 1（App 內是否深連結 composer 只記錄） |
| UI-8 | P0 | FR-16 | 來源紀律（FR-16）：S2 截圖抽查 10 筆結果頁，「查核來源」區每項皆帶 `tier_1_chip`／`tier_2_chip`，無 Tier 3 網域（用 OP-9 後的資料）；S3 截圖無 `news.google.com`；至少一張 S2「尚無查核機構證實」狀態截圖（SAFE 黃 + 橫幅）入檢核表 |

執行註記：實作票（`docs/rebuild/03_tickets.md` §0 第 4 條）沿用 mockup brief §2 的 token 名（`--c-red` 等），UI-3 的 grep 關鍵字實際執行時以該 token 名為準，判定準則（命中元件檔 ≤3、導覽／按鈕不出現紅黃綠）不變。

## 3.4 績效測試

執行方式：`evaluate.py --timing` + 後端結構化 log + 手動計時（依共識 §7 不新增腳本）。來源：spec §10.4。

| ID | 優先級 | 對應 FR | 通過準則 |
|----|--------|---------|----------|
| PF-1 | P0 | FR-01、三層快取、§9 效能 | `evaluate.py --timing`（web_search 關）：未命中 p50 ≤15 s／p90 <20 s（150 筆 `elapsed_ms`）；L1／L2 命中：對 10 筆已入庫文字各重送一次、從後端結構化 log 取 `elapsed_ms + cache_layer`，L1 p50 <1.5 s、L2 p50 <4 s |
| PF-2 | P0 | FR-01、三層快取 | 三層快取命中率：對 20 筆「改寫版謠言」（組員 C 準備，獨立於 eval_set）以 `POST /api/analyze/sync` 送出，log 中 `cache_layer=vector` ≥70% |
| PF-3a | P0 | FR-09、FR-10 | Threads 本專案可控延遲：從 `run_threads_poll` 取得 mention 到 `reply_to` 回 200 ≤60 s（log `elapsed_ms`；sim 與 live 各量 3 次） |
| PF-3b | P1 | FR-09 | Threads 端到端牆鐘時間（含 Meta 索引延遲，不可控）：使用者送出 @ 到回覆出現，記錄 5 次實測，目標 ≤ poll 間隔 + 60 s（demo 設 1 分鐘 → ≤2 分鐘）；未達標只記錄不擋 |
| PF-4 | P1 | FR-10 | 模擬模式 10 則 mention 一輪 <3 分鐘（AI 開）；mock AI 時 <10 s |
| PF-5 | P1 | FR-14、§9 成本 | 成本：150 筆評測總花費 ≤ USD 1.5；每次未命中平均 ≤ USD 0.02（CGU `GET /me/usage` 評測前後差 + OP-2 `usage` 輸出，記入 `docs/test/cgu_usage.txt`） |
| PF-6 | P1（可選） | FR-01（模型比較） | 模型比較組：同 150 筆以 `CGU_MODEL=gpt-5.4` 重跑一次（`USE_WEB_SEARCH=false`；預算 ≤ USD 2，先以 `/me/usage` 確認餘額 ≥ USD 4 才跑），與 `gpt-5.4-mini` 並列 accuracy／FN／p50 延遲／每筆成本；報告 §6 一張表；不作通過條件 |

## 3.5 品質測試

執行方式：`evaluate.py`（150 筆 `eval_set.csv`，SCAM／MISINFO／SAFE 各 50）。來源：spec §10.5。

| ID | 優先級 | 對應 FR | 通過準則 |
|----|--------|---------|----------|
| QA-1a | P0（閘門） | FR-01 | `evaluate.py --seed-db`（web_search 關）以 **CGU `gpt-5.4-mini`**（provider 鏈 `['cgu']`）重跑 150 筆：**FN=0（絕對條件）**；跑前後各查 `/me/usage`（PF-5） |
| QA-1b | P1（量測） | FR-01 | 同一次執行的 accuracy 與 FP 記錄實際值，目標 ≥93%／≤8；未達標不擋交付，但報告 §6 標示「未達標」並附錯誤分析。報告依 D-8 並列舊數字（0.96，gpt-5-mini via myai168，2026-07）與新數字（gpt-5.4-mini via CGU AIR，2026-09），各標 provider／model／date；PF-6 比較組（gpt-5.4）有跑才加第三欄 |
| QA-2 | P1 | FR-01（評測報告） | `evaluate.py` 報告加 `provider/model/date/use_web_search/elapsed_ms` 欄；`--report-only` 重算一致；`eval_errors.csv` 逐筆有 `error_type` |
| QA-3 | P0 | FR-15 | CI 每次 push 綠燈（`pytest` + `npm run build`）；`ci.yml` 不再寫死測試數 |
| QA-4 | P1 | FR-15、FR-16 | CLAUDE.md、README、`.env.example` 與本 spec 的端點／設定名一致（文件審查勾核表，含：Threads 權限清單修正、§2 表格「影片語音轉文字」列刪除、移除 `ENABLE_THREADS_BOT`／STT 設定鍵、`legacy/` 標示、**§2 AI 引擎改寫為「CGU AIR 為唯一 provider、myai168 已停用、額度查 `/me/usage`」、§9 加來源分級三條規則、`BOT_HANDLE=factcheck_tw_bot`、**§4／§7「runtime 變動不用 commit」補例外：FR-18 清洗後的 `knowledge_base.parquet` 提交一次作新種子**） |
| QA-5 | P0 | FR-18 | 清洗 dry-run diff 審查（FR-18）：`data/clean_sources_report.txt` 中 0 筆 `label_source="rule"` 被降級；隨機抽 10 筆降級列由負責人逐筆判斷，同意 ≥9（不同意者記回 `TIER1_DOMAINS`／規則修正後重跑 dry-run）；Cofacts 33 筆逐筆有「有回覆／無回覆」結論；Google News 31 筆逐筆有「解析成功→新網址／失敗→刪除」結論；審查紀錄存 `docs/test/clean_sources_review.md` |

# 4 測試方法

（待 O-15 撰寫：4.1 測試環境、4.2 通過準則、4.3 中止與再繼續條件、4.4 交付項目、4.5 時程。）

## 4.1 測試環境

（待 O-15：逐列依 spec §10.6「4.1 測試環境需求」。）

## 4.2 通過準則

（待 O-15：依 spec §10「4.2 通過準則彙總」。）

## 4.3 中止與再繼續條件

（待 O-15。）

## 4.4 交付項目

（待 O-15：依 spec §10.6「4.4 測試交付項目」。）

## 4.5 時程

（待 O-15。）

# 5 人員配置與訓練需求

（待 O-15；組員姓名待 O-10。）

## 5.1 人員配置

（待 O-15：以 spec §11 角色 A–D + 負責人 + 審查者列出。）

## 5.2 訓練需求

（待 O-15。）

# 6 測試結果與審查

（待 O-16：每個測試 ID 一列之結果總表、實際數字與審查簽核欄。）
